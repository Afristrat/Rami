# Ralph Progress — RAMI Complétion

## Codebase Patterns (à suivre)
- **Stack** : Next.js 16 (App Router, Turbopack), React 19 (React Compiler), TS strict, Tailwind v4, shadcn base-nova (@base-ui/react), Zod v4.
- **Proxy middleware** : `src/proxy.ts` (export `proxy()`), pas `middleware.ts`.
- **Supabase** : clients `src/lib/supabase/{server,client}.ts` ; service role `src/lib/supabase/service.ts` (bypass RLS). Auth SSR.
- **DB** : Drizzle (`packages/db/schema/*` ou `src/lib/db/`), migrations SQL `supabase/migrations/` (préfixe timestamp `2026...000NNN_`). Instance dédiée `supabase-db-szn6rjsrqig7n4oerw27egwr` (db-rami.ai-mpower.com).
- **RLS** : OBLIGATOIRE sur chaque table. `get_current_tenant_id()` lit `public.users.tenant_id`. Policy type `tenant_id = get_current_tenant_id()`.
- **Tenant resolution** : `resolveUserTenant(supabase, userId)` (tenants.owner_id → tenant_members → users.tenant_id).
- **LLM** : via proxy LiteLLM (`OPENAI_BASE_URL=https://proxy.ai-mpower.com/v1`, `OPENAI_API_KEY`). Texte = `deepseek-v4-flash` (`LLM_TEXT_MODEL`), vision = `moonshot-v1-8k-vision-preview` (`VISION_MODEL`, BASE64 only). Provider "openai" route vers le proxy. Config par cas d'usage : `src/lib/services/ai/prompt-config.ts` (DB `ai_prompts_config` + fallback).
- **Crawl4AI** : `CRAWL4AI_BASE_URL=https://crawl4ai.ai-mpower.com`, `POST /md {url, filter:"fit"}` → markdown.
- **Abstractions** : `PublishingProvider`, `ImageProvider` (5 providers fallback), `MetricsProvider` (`src/lib/services/metrics/` — types.ts + engagement.ts + index.ts router `collectMetricsFromPlatform`). OAuth refresh : `getValidToken()`.
- **Queue** : pg-boss connecté (schéma `pgboss`), jobs enregistrés dans `src/instrumentation.ts`. Jobs : `PUBLISH_POST` (publish-worker), `COLLECT_METRICS` (jobs/collect-metrics). Refresh OAuth partagé : `src/lib/queue/token-refresh.ts` (`getValidAccessToken(conn, supabaseService)`). `scheduleMetricsCollection({postId,tenantId})` planifie T+1h/24h/7j via `boss.send(..., {startAfter: seconds, singletonKey})`. Cron : `ATTRIBUTION_REFRESH` (jobs/attribution-refresh, `boss.createQueue`+`boss.work`+`boss.schedule('0 */6 * * *')`).
- **Attribution** : vue `attribution_facts` (security_invoker, lecture live RLS) + table cache `attribution_rankings` (RLS tenant). Service `src/lib/services/metrics/attribution.ts` (`topFeatures` live + `aggregateAttribution` pur). Dimensions extraites de `posts.ai_metadata` (`cognitive_objective`, `direction`, `dominant_color_hex`, `hook`, `format`, `brand_dna_score`) + heure (`scheduled_at`/`published_at`) + `platform`. `buildPerformancePrior(tenantId, sector)` → `PerformancePrior|null` (gaté `MIN_PRIOR_SAMPLE=10`).
- **Génération** : `compileBrandDNAToPrompts(brandDNA, brief, platform, seed, performancePrior?)` — prior pondère les couleurs (gagnantes en tête) + emphase style. `visual_session_images.performance_prior` (jsonb) persiste le prior. Table images = `visual_session_images` (PAS dans schema.ts Drizzle, SQL-only).
- **Storage** : MinIO (privé) + R2 (CDN). `src/lib/services/storage/`.
- **Crypto** : OAuth tokens AES-256-GCM (`OAUTH_TOKEN_ENCRYPTION_KEY`).
- **Conventions** : français accentué (commentaires/UI), `useWatch()` (pas `watch()`), Zod v4 `defaultValues` dans useForm (pas `.default()` avec zodResolver), `useCallback` retiré si React Compiler le gère, pas de composant créé en render.

## Pièges connus
- `output: "standalone"` conditionnel `BUILD_STANDALONE` (sinon casse `next start` Nixpacks).
- Moonshot vision : base64 inline uniquement (fetch+encode les URLs).
- deepseek : pas via `/v1/messages` (Anthropic) — uniquement `/v1/chat/completions`.
- Un compte créé hors onboarding n'a pas de tenant → 404 (créer tenants + users row).

## Déploiement
- Coolify app `rami` (uuid ry8ytnene4czxdhsoes0z56y), Nixpacks, auto-deploy sur push. API Coolify : token coffre.
- cloudflared tunnel nahda ; DNS via `cloudflared tunnel route dns --overwrite-dns 7156c3f9-... <host>`.

## CHECKPOINT — Prochaine story : US-010 (Job d'agrégation collective)
*Posé le 2026-05-31 après US-009. deps US-009 ✓ + US-011 (consentement opt-in) — US-011 est requise.*

**Ordre conseillé : US-011 (consentement opt-in RGPD) AVANT US-010** (US-010 ne doit agréger que les tenants consentants).
- **US-011** : flag de consentement par tenant (ex. colonne `tenants.collective_opt_in boolean default false` OU table dédiée) + UI réglages pour l'activer (vérif navigateur). RGPD : opt-in explicite, désactivable.
- **US-010** : job pg-boss `collective.aggregate` (quotidien, cron `boss.schedule`). Agrège `post_metrics` (dernier snapshot par post×plateforme, via le même pattern que attribution) par (secteur, culture, plateforme, metric) — secteur/culture lus depuis `tenants.brand_dna` (shape PLAT : `->>'sector'`, `->>'primaryCulture'`). N'inclut QUE les tenants opt-in (US-011) ET ne produit une ligne QUE si ≥ 5 tenants distincts (le CHECK k-anonymity bloquera sinon → filtrer AVANT insert). Upsert sur `(sector,culture,platform,metric)`. Test : <5 tenants → 0 ligne. Service client (bypass RLS).
  - metrics à produire : `avg_engagement`, `avg_impressions` (moyennes par tenant puis moyenne des tenants, ou moyenne globale — choisir moyenne des moyennes-tenant pour éviter qu'un gros tenant domine). `sample_size` = nb de tenants distincts.

## ⚠️ SPLIT ENVIRONNEMENT dev/prod (découvert US-011) — CRITIQUE
- `.env.local` (dev local) → `NEXT_PUBLIC_SUPABASE_URL=https://rfndjbrwpdltfzvyreyv.supabase.co` (instance Supabase **cloud hébergée**, séparée).
- **Prod** = `db-rami` (Coolify, conteneur `supabase-db-szn6...`) où j'applique les migrations via SSH.
- **Conséquence** : les migrations appliquées sur db-rami NE sont PAS sur l'instance dev cloud, et inversement. Un `npm run dev` local tape sur rfndjbrwpdltfzvyreyv (où `collective_optin` n'existe pas encore). db-rami n'a QUE le super-admin (mot de passe non accessible — anti-leak). → **Vérification navigateur authentifiée non triviale** : nécessite soit appliquer les migrations aussi sur l'instance dev + un compte test connu, soit un déploiement preview, soit un magic-link via service-role.
- **DÉCISIONS Amine (2026-05-31)** : (1) migrations sur **db-rami uniquement** (prod = source de vérité, ne PAS synchroniser l'instance cloud). (2) browser-verify via **compte test + dev local pointé sur db-rami**.

## CHECKPOINT — Vérif navigateur US-011 (à exécuter en session fraîche)
US-011 = **implémenté + RLS testée db-rami** mais `passes=false` tant que la vérif navigateur n'est pas faite. Plan d'exécution :
1. **Créer un compte test sur db-rami** (n'a que le super-admin). Via l'API admin GoTrue (service-role) — récupérer la SERVICE_ROLE_KEY depuis l'env du conteneur `rami` côté serveur SANS l'afficher (tout en un seul SSH), POST `http://supabase-kong-szn6...:8000/auth/v1/admin/users` `{email:"test-ralph@rami.local", password:<connu, ex 'TestRalph2026!'>, email_confirm:true}`. Puis SQL : `INSERT public.tenants(slug,name,owner_id,plan)` + `INSERT public.users(id,email,tenant_id,role,onboarding_completed)` + `UPDATE tenants SET brand_dna='{"sector":"tech"}'::jsonb` (sinon 404 tenant, cf. GOTCHA). Imprimer uniquement l'user id.
2. **Dev local → db-rami** : lancer `npm run dev` avec override env (NE PAS modifier .env.local) : `NEXT_PUBLIC_SUPABASE_URL=https://db-rami.ai-mpower.com` + anon key db-rami (à récupérer depuis l'env conteneur rami, non-secret). En PowerShell : `$env:NEXT_PUBLIC_SUPABASE_URL=...; $env:NEXT_PUBLIC_SUPABASE_ANON_KEY=...; npm run dev`.
3. **Playwright** (MCP) : login test-ralph → `/settings/profile` → vérifier la section « Intelligence collective » + screenshot → toggle ON → recharger → confirmer persistance (DB `tenants.collective_optin=true`) → toggle OFF.
4. Nettoyer le compte test si souhaité (`DELETE FROM auth.users WHERE email='test-ralph@rami.local'` → CASCADE).
5. Si OK → US-011 `passes=true` + commit `[US-011] ... browser-verified`.

Ce compte test servira aussi pour US-012 (dashboard), US-031, US-032 (toutes UI, browser-verify requis).

## ⚠️ US-027 — 2 BLOCKERS EXTERNES découverts (2026-06-01, on-LAN) — passes reste false
Tentative de browser-verify on-LAN → code US-027 **confirmé correct**, mais happy-path bloqué par 2 facteurs EXTERNES (pas des défauts code) :
1. **Table `leads` ABSENTE de db-rami** : `to_regclass('public.leads')` = NULL. Le module CRM (leads + lead_activities + enum `lead_stage` + RLS) est dans `src/lib/db/schema.ts` mais **n'a AUCUNE migration** (`supabase/migrations/` n'en contient pas) → jamais déployé en prod. `getLeadsAction`/`createLeadAction` (Drizzle) échouent (« Failed query… leads ») → la page tombe en fallback `DEMO_LEADS`. **GAP PROD à traiter** (story dédiée : écrire + appliquer migration CRM sur db-rami). Bloque aussi tout browser-verify Leads (US-028/029).
2. **Clé Apollo → HTTP 403** : la clé fournie n'a PAS accès à l'endpoint People Match (restriction de plan Apollo, ou clé inactive). **Méthode d'auth confirmée CORRECTE** : Apollo exige `X-Api-Key` en header (vérifié — `api_key` en body → 422 « must be passed in the X-Api-Key header ») = exactement ce que fait `apollo.ts`. Donc `enrichViaApollo` dégrade proprement en `reason:"error"` (HTTP 403). Happy-path réel nécessite un plan Apollo avec accès API Match.
→ **Code US-027 = correct + unit-testé (10/10) + méthode auth validée contre l'API réelle.** passes=false car « vérifié dans le navigateur » (happy-path) impossible tant que (1) migration leads + (2) plan Apollo non résolus.

### Provisioning clé Apollo (fait 2026-06-01, à la demande d'Amine) — 3 cibles
- **Coffre DPAPI Claude Code** : `APOLLO_API_KEY` (via `HermesVaultSync.psm1` `Set-VaultKey`, backup auto). 22 car.
- **Coolify prod app RAMI** : `APOLLO_API_KEY` (POST `/api/v1/applications/{uuid}/envs`, HTTP 201 ; ⚠️ champ `is_buildtime` PAS `is_build_time` ; redeploy requis pour runtime).
- **Vault Hermes** (credential-vault `vault.js`, conteneur `hermes-oagpd60vuec2zhokge4tf93v`, `/opt/data/home/.hermes-vault/`) : clé `apollo.api_key`, hash-vérifiée (`sha256(val+\n)==distant`). Passphrase = coffre `HERMES_VAULT_PASSPHRASE`. Méthode anti-leak : passphrase+valeur en base64 décodées côté conteneur, `HOME=/opt/data/home`, vérif par hash (jamais `get` brut).

### ⚠️ Fixtures tenant test modifiées (db-rami)
`test-ralph@rami.local` : `brand_dna.sector`=**finance_islamique** (+ palette vert/rouge/bleu, pour US-016) et `plan`=**agency** (pour atteindre la page Leads, gated AGENCY+). À garder en tête pour futurs verifies.

## ✅ Migration CRM appliquée db-rami (2026-06-01) — gap prod corrigé
`supabase/migrations/20260315000006_crm_leads.sql` : enums `lead_stage`/`lead_activity_type` + tables `leads` + `lead_activities` (fidèles au schéma Drizzle) + 5 index + RLS (2 policies `tenant_id = get_current_tenant_id()` USING+WITH CHECK). **Appliquée db-rami** (CREATE OK). **RLS testée cross-tenant** : isolation SELECT OK (userA ne voit que son tenant), WITH CHECK bloque l'insert cross-tenant (« new row violates row-level security policy »). → **Module Leads désormais fonctionnel en prod** (était cassé : aucune table). Débloque US-028/US-029 aussi.

## ✅ Conclusion Apollo (2026-06-01) : plan GRATUIT = AUCUN accès API REST
Les 4 endpoints testés (`people/match`, `mixed_people/search`, `organizations/enrich`, `mixed_companies/search`) renvoient **403 `API_INACCESSIBLE`** : *« not accessible with this api_key on a free plan. Please upgrade »*. Les 120 crédits du plan gratuit = app web Apollo, PAS l'API REST. → **L'enrichissement US-027 nécessite un plan Apollo PAYANT.** Code `apollo.ts` correct (auth `X-Api-Key` header confirmée par Apollo, dégradation 403→`reason:"error"` propre). Clé provisionnée (coffre/Coolify/Hermes). US-027 happy-path = bloqué uniquement par le plan Apollo payant.

## ✅ Abstraction EnrichmentProvider + BYOK + provider Hunter (2026-06-01)
Suite décision Amine (option B + BYOK, sans altérer Apollo). `src/lib/services/leads/` : `types.ts` (`LeadEnrichment`/`EnrichmentResult`/`EnrichInput`/`EnrichmentProviderId`), `hunter.ts` (Hunter Email Finder, `mapHunterData` pur + `enrichViaHunter(input, apiKey)` clé injectée), `index.ts` (router : `getActiveEnrichmentProvider` env `LEADS_ENRICHMENT_PROVIDER` défaut apollo ; `resolveEnrichmentKey` = BYOK `ai_provider_keys` cat='enrichment' déchiffré `decryptToken` → fallback env ; `enrichLead` dispatch). `apollo.ts` **INCHANGÉ** (clé via env). `enrichLeadAction` appelle `enrichLead`. Migration `20260315000007_enrichment_byok.sql` (cat 'enrichment' + rows apollo/hunter dans ai_provider_keys, **appliquée db-rami**). `.env.example` : `LEADS_ENRICHMENT_PROVIDER` + `HUNTER_API_KEY`. Tests Jest `tests/unit/hunter-enrich.test.ts` (15). Doc `docs/ENRICHMENT_PROVIDERS_BYOK.md` (Apollo/Hunter/PDL/Dropcontact/Enrich.so). Gates TS0/lint0/build/jest OK. **Pour valider happy-path US-027 sans coût** : créer une clé Hunter gratuite (cf. doc), la poser (BYOK ai_provider_keys ou env HUNTER_API_KEY) + `LEADS_ENRICHMENT_PROVIDER=hunter`, puis browser-verify (table leads existe désormais).

## ✅ US-027 BROWSER-VERIFIED (2026-06-01, on-LAN, provider Hunter + BYOK) — passes=true
Happy-path validé de bout en bout en navigateur via le provider **Hunter.io gratuit** + **BYOK-DB chiffré** :
- Clé Hunter (40 hex) fournie par Amine (presse-papier) → stockée coffre `HUNTER_API_KEY` + **chiffrée AES-256-GCM** (OAUTH_TOKEN_ENCRYPTION_KEY de l'app) dans `ai_provider_keys` (provider hunter, cat enrichment) sur db-rami. Round-trip déchiffrement vérifié.
- Dev local : NEXT_PUBLIC+anon+service-role+OAUTH key db-rami + tunnel Postgres + `LEADS_ENRICHMENT_PROVIDER=hunter`, **SANS `HUNTER_API_KEY` env** → la clé est résolue UNIQUEMENT via le BYOK-DB chiffré (`resolveEnrichmentKey` → `decryptToken`).
- Parcours : créer lead « Box / Aaron Levie » (Drizzle via tunnel OK, table créée) → bouton Enrichir → `enrichLeadAction` → `enrichLead` (router) → Hunter Email Finder → **`email=alevie@box.com`** récupéré + backfill du champ email vide + `apollo_data` rempli (organization=Box). Vérifié en DB. Log `lead_enriched`.
- Cleanup : lead test supprimé (pas de PII résiduelle), dev+tunnel arrêtés, env temp purgé.
- Endpoints Hunter confirmés conformes (presse-papier d'Amine : `/v2/email-finder?domain=&first_name=&last_name=&api_key=` = exactement mon implémentation).
- **Clé Apollo** : reste 403 sur plan gratuit (enrichissement Apollo = plan payant) ; provider Hunter sert de défaut gratuit. Pour activer Hunter en PROD : poser `LEADS_ENRICHMENT_PROVIDER=hunter` (Coolify) + redéployer (la clé Hunter BYOK est déjà en DB).
- **Follow-up cosmétique (mineur)** : le libellé bouton « Enrichir via Apollo » + le log « Lead enrichi via Apollo » mentionnent Apollo alors que le provider peut être Hunter → rendre provider-neutre (i18n `apolloEnrichButton` → `enrichButton`). Hors-scope US-027 (fonctionnel OK).

## CHECKPOINT (CLOS) — Browser-verify US-027
*US-027 = impl complète + gates verts (TS0/lint0/build/jest 10), passes=false tant que la vérif navigateur (happy-path) n'est pas faite. Prérequis : (1) migration CRM `leads` appliquée sur db-rami, (2) clé Apollo avec accès People Match.*
1. Poser `APOLLO_API_KEY` (réelle) en env du dev local (ou confirmer qu'elle est sur Coolify pour la prod). Sans clé → le bouton renvoie « Enrichissement Apollo non configuré ».
2. Dev local → db-rami (cf. méthode US-011, override `NEXT_PUBLIC_*`). Créer/avoir un lead réel pour le tenant test (KanbanBoard → « Ajouter un lead » avec un contact identifiable, ex. nom + entreprise connus d'Apollo).
3. Playwright : ouvrir le lead → onglet Infos → bloc « Enrichissement Apollo » → cliquer **Enrichir via Apollo** → vérifier spinner puis : `apollo_data` rempli (badge « Mis à jour »), champs vides complétés (email/secteur/taille/localisation), bouton devient « Réenrichir ». Tester aussi un contact introuvable → message « Aucune correspondance Apollo ». Screenshot.
4. Si OK → US-027 `passes=true` + `completedAt` + commit `[US-027] browser-verified`.

## ✅ US-016 BROWSER-VERIFIED (2026-06-01, on-LAN) — passes=true
Vérif navigateur exécutée (dev local override `NEXT_PUBLIC_*`→db-rami, login test-ralph, `/dashboard/brand-dna`). Tenant test posé en `finance_islamique` + palette `vert_emeraude`/`rouge_passion`/`bleu_marine` (SQL db-rami). Badge `cultural-score-badge` rendu : **score 70/100 « Bonne »** (70+10+10−20), vert_emeraude→recommandée, rouge_passion→à éviter + raison + **alternative Bordeaux Premium**, bleu_marine→recommandée. **Switch FR↔EN OK** (« Cohérence culturelle/Bonne » ↔ « Cultural coherence/Good », recommended/to avoid/Suggested alternative). **DETTE i18n corrigée au passage** : les 5 raisons `brandDna.sectorColorRules.*` étaient en FR dans en/es/de/pt/tr/zh (ar déjà traduit) → traduites dans les 7 locales (les NOMS de couleurs « Vert Émeraude » restent FR = noms propres de marque, cohérent avec le picker). ⚠️ Le tenant test a désormais `brand_dna.sector=finance_islamique` (et non plus `tech`) — fixture pour futurs browser-verify Brand DNA.

## CHECKPOINT (CLOS) — Browser-verify US-016
*US-016 = impl complète + tous gates auto verts (TS0/lint0/build/jest 90/90), passes=false tant que la vérif navigateur n'est pas faite. Bloquée cette session : poste hors réseau serveur (SSH timeout).* 
Plan d'exécution (réutilise la méthode US-011, **PAS de tunnel Postgres** car page Brand DNA = Supabase HTTPS) :
1. **Re-vérifier l'IP LAN du serveur** (DHCP dynamique) : `ssh ... hostname`. Récupérer l'anon key db-rami via `docker exec rami printenv NEXT_PUBLIC_SUPABASE_ANON_KEY` (côté serveur, sans l'afficher) OU coffre si documentée.
2. **Préparer le tenant test** `test-ralph@rami.local` : poser une palette riche pour exposer les 3 verdicts. Ex. via SQL (`UPDATE public.tenants SET brand_dna = jsonb_set(...) WHERE ...`) OU via le wizard in-app : `sector=finance_islamique`, `colorPrimary=vert_emeraude` (recommandée), `colorSecondary=rouge_passion` (à éviter → alternative bordeaux_premium), `colorAccent=bleu_marine` (recommandée). Score attendu = 70 (70+10−20+10), niveau **good**.
3. **Dev local → db-rami** (sans toucher .env.local) : `$env:NEXT_PUBLIC_SUPABASE_URL='https://db-rami.ai-mpower.com'; $env:NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon db-rami>; npm run dev`. ⚠️ tuer un éventuel dev zombie sur :3000 avant.
4. **Playwright (MCP)** : login test-ralph → `/dashboard/brand-dna` → vérifier la carte `data-testid="cultural-score-badge"` dans la Section 2 « Palette Causse » : score **70 /100**, niveau « Bonne », ligne **vert_emeraude → recommandée**, ligne **rouge_passion → à éviter** + raison (« Symbolisme culturel… ») + **alternative Bordeaux Premium**, ligne **bleu_marine → recommandée**. Screenshot. Switch FR↔EN (title « Cohérence culturelle » / « Cultural coherence »).
5. Si OK → US-016 `passes=true` + `completedAt` + commit `[US-016] browser-verified`.

## brand_dna : shape RÉELLE (plate) — IMPORTANT
Le brand_dna sauvegardé par l'onboarding est **PLAT**, PAS nesté. Clés réelles : `sector`, `primaryCulture`, `objectifCognitif`, `objectifsCognitifs`, `colorPrimary`/`colorSecondary`/`colorAccent`, `brandName`, `tagline`, `positioning`, `voiceTone`, `typography`, `audienceAge`/`audienceLocation`/`audienceDescription`/`audiencePainPoints`, `logoDataUrl`, `sectorCustom`, `perplexity_benchmark`. ⚠️ L'interface `BrandDNA` du prompt-compiler suppose une shape NESTÉE (`identity.sector`, `color_palette[]`, `culture_markets.primary_culture`) → **divergence** : le compiler lit probablement mal les couleurs en prod (dette pré-existante, hors-scope US-009, à traiter dans une story dédiée). Pour US-010 lire le secteur via `brand_dna->>'sector'` et la culture via `brand_dna->>'primaryCulture'`.
- Helper SQL créé (US-009) : `get_current_tenant_sector()` = `brand_dna->>'sector'`.

## Méthode migration db-rami (validée US-007/008/009)
Appliquer une migration SQL sur l'instance dédiée db-rami **sans casser le copier-coller** :
```powershell
. "C:\Users\amans\.claude\secrets\load-secrets.ps1" | Out-Null
$key=$env:SERVER_SSH_KEY;$h=$env:SERVER_HOST;$u=$env:SERVER_SSH_USER
$db="supabase-db-szn6rjsrqig7n4oerw27egwr"
$sql=[IO.File]::ReadAllText("<chemin migration .sql>")
$blob=[Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes($sql))
ssh -i $key -o BatchMode=yes "$u@$h" "echo '$blob' | base64 -d | docker exec -i $db psql -U postgres -d postgres -v ON_ERROR_STOP=1"
```
- Test RLS cross-tenant : `BEGIN; INSERT tenant B temp + lignes A/B; SELECT set_config('request.jwt.claims','{""sub"":""<userA>""}',true); SET LOCAL ROLE authenticated; SELECT ... (doit voir A seul); ROLLBACK;`. `get_current_tenant_id()` = `SELECT tenant_id FROM users WHERE id=auth.uid()`. Tenant réel unique : `ai-mpower` `0f38ef43-7467-494b-a2a4-86a21d3293bd`, user super-admin `6d7bda98-44b0-4735-81f4-e9f7ff8274aa`.
- ⚠️ Dollar-quoting (`$$ ... $$`) cassé par l'échappement PowerShell→ssh : éviter les blocs `DO $$`, préférer des INSERT directs pour tester WITH CHECK.
- `service_role`/Supabase : les grants anon/authenticated/service_role sont **hérités automatiquement** sur les nouvelles tables (ALTER DEFAULT PRIVILEGES Supabase) → pas de GRANT à ajouter.

## CHECKPOINT — Prochaine story : US-025 (Offre commerciale PDF, Epic D, P1, deps [])
*Posé 2026-06-04 (fin session #6). 21/50. Epic A/B/E complets ; US-020/021 OPS ; US-022 impl+pipeline OK (passes=false, happy-path bloqué clé Whisper). **Zéro dette connue** (les 2 dettes brand_dna/AiRecommendations résolues). Session #6 très dense (2 dettes + US-022 + browser-verify) → **session fraîche recommandée** pour US-025.*

**US-022 = impl complète + pipeline browser-verified** (cf. log) — débloquer le happy-path = fournir `WHISPER_API_KEY` (vraie clé OpenAI) ou configurer whisper-1 sur le proxy LiteLLM (actuellement HTTP 400). US-023/024 dépendent de ce déblocage.

**Plan US-025 (Offre commerciale PDF)** : entièrement autonome (LLM deepseek via proxy ✓ + PDF print-CSS, pas de clé externe).
1. Vérifier l'existant : page `/dashboard/documents` (factice, `requireFeature('document_engine')` Agency+), table `documents` (schema.ts : type `offre_commerciale`, `content_json`, migration ?), `documents.actions.ts`.
2. Générer le contenu de l'offre via `callTextLLM` + `getPromptConfig` (nouveau fallback `document_commercial_offer`) à partir d'un brief + Brand DNA (utiliser `normalizeBrandDNA`).
3. Rendu : page web de l'offre (style Gamma) + export PDF via `window.print()` print-CSS (pattern color-trends, zéro dep).
4. Migration table `documents` si absente (RLS tenant). Action `createCommercialOfferAction`. Gates + browser-verify.
Puis US-026 (rapport client PDF, deps US-001 ✓), US-030 (competitors Crawl4AI ✓), US-032 (approval workflow), US-033/034/035 (OPS).

**Méthode browser-verify on-LAN (session #5, validée, réutilisable)** : tunnel SSH conteneur PG `172.24.0.6:5432` (Git Bash, secrets via `docker exec <db|app> printenv`, jamais affichés ; conteneur app = `ry8ytnene4czxdhsoes0z56y-105027232246`) + `npm run dev` override SUPABASE_DB_URL(tunnel)+NEXT_PUBLIC db-rami+anon+service-role+OAUTH+OPENAI. Login déjà actif (cookies test-ralph) OU `test-ralph@rami.local`/`TestRalph2026!`. Playwright MCP (plugin `mcp__plugin_playwright_playwright__*`). Cleanup systématique (fixtures restaurées, dev+tunnel tués).

## CHECKPOINT (CLOS) — US-025 browser-verified PROD le 2026-06-11 → passes=true (31/58)
*Méthode browser-verify PROD validée (remplace l'on-LAN interdit) : Playwright MCP directement sur https://rami.ai-mpower.com avec le compte test-ralph. Migration appliquée via SSH (retour on-LAN en cours de session) + RLS SQL testée. Prochaine story : US-026 (rapport client PDF, deps US-001 ✓).*

## Log itérations
<!-- (date | story | fichiers | learnings) -->
- **2026-06-14 | anti-factice RGPD | bouton « Exporter mes données » câblé (commit `64f9843`, BROWSER-VERIFIED PROD)** — Le bouton « Exporter toutes mes données » (zone de danger, `general-settings-client.tsx:675`) n'avait **aucun handler** → obligation légale RGPD Art. 20 / CNDP loi 09-08 (portabilité) non remplie. Créé `src/lib/actions/data-export.actions.ts` (`exportMyDataAction`) : rassemble profil, tenant+brand_dna, posts, leads, documents, transcriptions, connexions sociales (métadonnées) en JSON structuré tenant-scopé, **tokens OAuth chiffrés EXCLUS** ; bouton câblé → Blob JSON + téléchargement `rami-export-AAAA-MM-JJ.json` (état loading + erreur i18n). **Browser-verified PROD** (test-ralph) : clic → `rami-export-2026-06-14.json` (9,7 Ko) téléchargé, top keys export_metadata/profile/tenant/posts/leads/documents/transcriptions/social_connections, email+tenant réels, **`access_token` absent du JSON** (vérifié), note de sécurité présente. i18n +1 ×8.
- **2026-06-14 | anti-factice Transcriptions | fin du mock verbatims/speakers/résumé (commit `5e098b8`, BROWSER-VERIFIED PROD)** — Le détail d'une transcription (`TranscriptionResult` + `AiSummaryPanel`) affichait des données INVENTÉES en l'absence de vraie transcription : `MOCK_SPEAKERS` (dialogue « Réunion Stratégie Q3 », Intervenant 1/2), `MOCK_SUMMARY`, `MOCK_VERBATIMS`, `MOCK_ACTIONS`, titre/durée factices (`2712`s), `KEYWORDS` inventés, waveform audio simulée (`Math.sin`) + bouton lecture non câblé. La page détail rendait même le mock pour un id `mock-`. Corrigé : (1) `TranscriptionResult` ne lit QUE les vraies données ; **états honnêtes par statut réel** (`failed`→carte d'erreur, `processing/uploading`→carte « en cours », `completed` sans diarisation→texte brut réel, sans texte→vide) ; surlignage du **seul terme recherché** (KEYWORDS supprimés) ; waveform + play factices retirés ; (2) `AiSummaryPanel` : résumé/verbatims/actions réels ou états vides (« Résumé non disponible », « Aucun verbatim extrait », « Aucune action identifiée ») ; (3) page détail → **vrai `notFound()`** si la transcription n'existe pas (fini la démo). i18n +8 clés ×8. Gates TS0/lint0/Jest336/build. **Browser-verified PROD** (seed 1 `completed` + 1 `failed`) : completed → texte transcrit réel + panneau IA en états vides (mockFound=[]) ; failed → « La transcription a échoué » + durée `--:--`. Cleanup vérifié 0. *Learning* : une expression `?? []` réévaluée à chaque render et passée en dépendance de `useMemo` déclenche le warning React Compiler exhaustive-deps → la stabiliser elle-même dans un `useMemo`.
- **2026-06-14 | BUG BLOQUANT workflow (signalé Amine) : Étape 2 → 3 impossible (commit `5c11e32`, BROWSER-VERIFIED PROD)** — Amine : « à partir de l'étape 3 les éléments n'existent pas ». Cause RÉELLE : on était **bloqué à l'étape 2**. `Step2Platforms` initialisait `selectedFormat` à `undefined`, or `step2Schema.format = z.enum(CONTENT_FORMATS)` est **requis** → « Suivant » échouait avec l'erreur Zod brute « Invalid option: expected one of post|carousel|story|reel|article » et restait sur l'étape 2 (l'étape 3 paraissait « ne pas exister » car inatteignable). Fix : `selectedFormat` pré-sélectionné à `"post"` (format universel) par défaut → progression jamais bloquée ; messages d'erreur platforms/format passés en i18n (`platforms.platformRequired`/`formatRequired` ×8, fini le message Zod brut anglais). **Browser-verified PROD** : Step1 → Step2 (LinkedIn) → Suivant → **Étape 3 atteinte**, légende LinkedIn réelle générée par le LLM, zéro erreur. *Learning* : tout champ Zod `enum` requis dans un wizard DOIT avoir une valeur initiale valide (défaut), sinon le bouton Suivant bloque silencieusement avec un message technique — vérifier chaque step du wizard pour ce piège.
- **2026-06-14 | anti-factice honnêteté | Sous-titre/loader Étape 3 « Claude Haiku » → provider-neutre (i18n ×8)** — `workflow.step3.desc` (« Claude Haiku génère vos captions ») et `workflow.textGen.generatingCaptions` mentionnaient un modèle FAUX (réel = deepseek-v4-flash via proxy). Remplacés par « L'IA génère vos captions… » ×8. Plus aucune mention « Claude Haiku » dans `messages/` (reste `video.pipeline.subtitle` sur la page vidéo mock = lot #9 à part).
- **2026-06-14 | anti-factice Documents download + sidebar Connexions (commit `6d8f935`, BROWSER-VERIFIED PROD)** — (#5) `DocumentsTable.tsx` : les 2 boutons « Télécharger » (vue liste + vue grille) appelaient `handleView` (ouvraient la page de détail) au lieu de télécharger → désormais `window.open('/dashboard/documents/[id]/pdf')` (route PDF serveur réelle `@react-pdf/renderer`, livrée session #7). **Browser-verified PROD** : clic Télécharger sur l'offre « Takaful Atlas Assurance » → **vrai fichier `offre-360-….pdf` téléchargé, 26 Ko, magic `%PDF-1.3`** (PDF brandé valide). → l'infra de téléchargement PDF est prouvée (couvre aussi le gate PDF d'US-026). (#8b) `nav-config.ts` : ajout de l'entrée **Connexions** (`/settings/connections`) à `navMain` — la page OAuth existait mais était introuvable depuis la navigation. **Browser-verified** : lien « Connexions » présent dans la sidebar. i18n +`nav.connections` ×8. Gates TS0/lint0/build.
- **2026-06-14 | anti-factice Approbations | Kanban réel + décisions persistées (commit `18fbd1f`, BROWSER-VERIFIED PROD)** — `approval-board.tsx` affichait **`MOCK_ITEMS`** (10 posts inventés : Sarah Benali, Karim Tazi, Fatima Ouardi, Youssef Alami…) avec des handlers **locaux** (approve/reject ne touchaient que le `useState`, zéro persistance). Remplacé par du réel : (1) nouveau statut post **`rejected`** (migration `…015` `ALTER TYPE post_status ADD VALUE` appliquée db-rami + ajouté à l'enum Drizzle) pour tracer les refus ; (2) `src/lib/actions/approval-board.actions.ts` : `getApprovalBoardAction` (posts review/approved/rejected du tenant + **nom réel de l'auteur** via jointure `users` + commentaire de revue depuis `ai_metadata.review_comment`), `decideInternalApprovalAction` (approve→approved / reject→rejected, commentaire persisté, tenant-scopé + garde d'état), `reopenForReviewAction` (Modifier → repasse en review) ; (3) bouton **Publier** câblé sur le vrai `publishPost` (enqueue pg-boss) ; (4) board : chargement `useEffect` + **refresh depuis la DB après chaque action** (reflète la vérité), états loading/vide honnêtes, toasts réels. i18n +approveSuccess/rejectSuccess/publishSuccess/actionError −publishPlaceholder ×8. Gates TS0/lint0/Jest336/build. **Browser-verified PROD** (test-ralph, seed 2 posts en review créés par Test Ralph) : board affiche les 2 vrais posts (auteur « Test Ralph », plus aucun nom mock) → **Approuver** le 1er → passe en colonne Approuvé (DB `approved`) → **Rejeter** le 2e avec commentaire → passe en Rejeté (DB `rejected` + `ai_metadata.review_comment` = le commentaire saisi, **vérifié en SQL**) → colonnes vides honnêtes ailleurs. Cleanup seed vérifié 0 post. *Learnings* : (a) `ALTER TYPE … ADD VALUE` doit tourner hors transaction (autocommit psql) et est utilisable immédiatement ; (b) un commentaire de revue sans colonne dédiée se loge proprement dans `ai_metadata` (jsonb) sans migration de colonne.
- **2026-06-14 | anti-factice Dashboard | Répartition plateformes réelle + retrait faux trend (commit `25048a1`, BROWSER-VERIFIED PROD)** — `platform-distribution.tsx` affichait **45/30/15/10 codés en dur** (`platforms` const) avec barres pleines mensongères malgré « Total de 0 contenus ». `BrandDnaKpiCard.tsx` affichait « **+2% vs semaine dernière** » (`trendVsLastWeek`) sans aucun historique. Corrigé : (1) la page dashboard calcule la **répartition RÉELLE** (agrégation des `posts.platforms` du tenant → occurrences par plateforme → pourcentages triés desc) et la passe en prop `distribution` ; `PlatformDistribution` rend des barres à **largeur dynamique** (`style width`, plus de classe Tailwind statique `w-[45%]`), couleur par plateforme, **état vide honnête** « Aucune donnée de répartition » si 0 post ; (2) faux trend retiré du KPI (le score % reste réel) ; (3) i18n −`trendVsLastWeek` +`platformDistributionEmpty` ×8 ; retrait import `count` Drizzle inutilisé. Gates TS0/lint0/0. **Browser-verified PROD** (test-ralph) : 0 post → « Aucune donnée de répartition » + KPI sans trend ; puis **seed 3 posts** (linkedin×2, twitter×1, instagram×1) → barres **LinkedIn 50% / X 25% / Instagram 25%, « Total de 3 contenus actifs »** (= calcul exact), cleanup vérifié 0 post. *Learning* : largeurs dynamiques = `style={{width}}` (Tailwind ne génère pas les valeurs arbitraires au runtime).
- **2026-06-14 | Persistance content_sessions — BROWSER-VERIFIED PROD (point 0 NEXT soldé)** — Vérif navigateur en prod (Playwright MCP, test-ralph) : Step 1 rempli (titre + objectif Confiance) → transition Step 2 → indicateur « Brouillon enregistré » (autosave DB réel) → refresh → **bannière « Reprendre votre brouillon ? »** avec titre + « étape 2 sur 7 » + horodatage exacts → « Reprendre » → état restauré (Step 1 complété, position Step 2). Cleanup `DELETE FROM content_sessions WHERE tenant_id='12fe935c-...'` (vérifié 0 restant). La persistance du workflow est RÉELLE et fonctionnelle.
- **2026-06-14 | LOT 1bis anti-factice | WorkflowSidebar — vrai Brand DNA + score réel + historique réel (commit `13c6240`, BROWSER-VERIFIED PROD)** — Le `WorkflowSidebar.tsx` affichait un **mock DEFCON1 sur TOUTES les étapes** de Créer un post : score d'alignement `0.87` codé en dur, couleurs `#001D4A/#D4AF37/#F8FAFC` factices, 3 posts d'historique inventés (« Annonce levée de fonds Série A — Il y a 2h »…). Le parent `WorkflowClient` rendait `<WorkflowSidebar />` **sans aucune prop** → fallback mock systématique. Corrigé : (1) nouvelle server action `getWorkflowSidebarDataAction()` (`visual.actions.ts`) lit le vrai `brand_dna` (`normalizeBrandDNA` → couleurs HEX réelles, `active_platforms`, `editorial_tone.register`) + calcule un **score de cohérence culturelle RÉEL** (`scoreCulturalCoherenceFromHex` palette × secteur, US-016) affiché seulement si `hasRules` + lit les **posts récents réels** du tenant (table `posts`, limit 5) ; (2) `WorkflowSidebar` devient **présentationnel pur** (props `brandDNA`/`history`/`loading`, zéro fallback mock, états vides honnêtes « Brand DNA non configuré » + lien /brand-dna et « Aucun post récent ») ; le « Voir tout l'historique » devient un vrai `<Link href="/dashboard/calendar">` ; (3) `WorkflowClient` charge via `useEffect` + passe les props aux 2 instances (guidé + expert). i18n : +`noBrandDna`/`configureBrandDna`/`noHistory`, −`defaultBrand`/`defaultTone`, **+ traduction des 6 clés sidebar restées en anglais pour es/de/pt/tr/zh** (dette pré-existante corrigée). Gates TS0 · lint0/0 · Jest 336/336 · build OK. **Browser-verified PROD** (test-ralph, après auto-deploy Coolify) : palette **#059669/#DC2626/#1E3A5F** (vraies couleurs Causse finance_islamique), ton « expert », **score 70/100** (= cohérence culturelle réelle, identique au calcul US-016), historique « **Aucun post récent** » (0 post). Plus AUCUN mock. *Learnings* : (a) un fichier `'use server'` peut exporter des `interface`/`type` (effacés à la compilation, pas de violation runtime) → importables via `import type` côté client ; (b) `WorkflowClient` rendait la sidebar à 2 endroits (mode guidé L583 indent 8 + mode expert L439 indent 10) → `replace_all` n'attrape pas les 2 si l'indentation diffère, vérifier au grep ; (c) le tenant test-ralph n'a pas d'`active_platforms` renseignées → la sous-section plateformes se masque proprement (état honnête).
- **2026-06-12 | LOT 1 anti-factice | Workflow « Créer un post » Steps 4→7 câblés (4 commits, browser-verify prod en cours)** — Suite de l'audit `docs/AUDIT_FACTICE_2026-06-12.md` (Step1/Step3 faits session #8). **Step 4** (`ead863c`) : module partagé `src/lib/services/image-generation/style-presets.ts` (12 presets id→fragment de prompt anglais, résolution serveur, id forgé ignoré) injecté dans `generateVisualContentAction(…, {stylePresetId})` ; chips sélectionnables + hint honnête « Régénérer pour appliquer » ; `Step4Data.stylePresetId` persisté ; code mort retiré (boutons Presets/Custom, tabs objectif, badge provider `placeholder` retiré aussi du type union) ; libellé « via Fal.ai » mensonger → clé `generating`. **Step 5** (`d5ce51c`) : module PUR `src/lib/services/workflow/quality-score.ts` (4 métriques RÉELLES : longueur vs limite plateforme, brandDnaScore réel du visuel, hashtags vs fourchette/plateforme `HASHTAG_RANGES`, détection CTA FR/EN `detectCta` ; score pondéré 0-100 + grade, dépassement limite → plafonné D) ; fin du « A+ »/jauges 85-60 % inventés ; « Enregistrer en brouillon » câblé (`saveWorkflowPostAction` draft) ; « + Ajouter » hashtag réel (input inline) ; retraits honnêtes : bouton UTM sans logique, métriques imageResolution/colorContrast non calculables, « Analyse IA » → « Contrôles automatiques ». **Step 6** (`58e0ec2`) : table `approval_tokens` (migration `20260315000013` **appliquée db-rami + RLS cross-tenant testée** SELECT 0/INSERT rejeté) ; module PUR `approval-link.ts` (actionnabilité, validation forme token, URL) ; `createApprovalLinkAction` (post en review + token 32 octets base64url, TTL 14 j) ; page publique `/approve/[token]` (noindex, lookup service-role, décision Approuver/Réviser + commentaire → maj token + posts.status approved/draft, idempotent) ; bouton Copy réel ; faux « Approbateur désigné — Équipe Client » retiré ; **media_urls enfin persisté** (createPost/updatePost/saveWorkflowPostAction) ; Step 7 fait un UPDATE du post du Step 6 (`existingPostId`) au lieu d'un doublon. **Step 7** (`6bc6254`) : module PUR `optimal-time.ts` (fenêtres d'engagement par plateforme — heuristique bonnes pratiques JAMAIS présentée comme stat tenant — modulées par objectif cognitif matin/soir, délai min 30 min, déterministe) ; fausse suggestion « LinkedIn lundi 8h30 +14 % » supprimée ; bouton « Utiliser cette suggestion » câblé. **FIX DEFCON découvert** : la planification datée était CASSÉE — `datetime-local` (« YYYY-MM-DDTHH:mm ») rejeté par `z.string().datetime({offset:true})` → conversion `toISOString()` + validation ; label « Heure (UTC) » erroné → « Heure » (locale) ; toggle « Multidiffusion synchronisée » sans effet retiré. i18n ×8 à chaque step (scripts node temporaires, JSON.stringify 2 espaces préserve l'ordre). Gates à chaque commit : TS 0 · lint 0/0 · Jest **329/329** (40 tests ajoutés : style-presets 6, quality-score 13, approval-link 11, optimal-time 10) · build OK. *Learnings* : (a) `z.string().datetime({offset:true})` rejette silencieusement la valeur d'un `<input datetime-local>` — toujours convertir en ISO complet côté client ; (b) la page publique `(public)` hors provider i18n client → passer les labels traduits en props depuis le Server Component (`getTranslations`) ; (c) i18n en masse : script node JSON.parse/stringify(.,null,2) = diff minimal (l'ordre des clés JS est préservé).
- **2026-06-11 | US-025 | Offre commerciale PDF (BROWSER-VERIFIED PROD, passes=true, 31/58)** — Module PUR `src/lib/services/documents/commercial-offer.ts` (`CommercialOfferContentSchema` Zod, `buildOfferSystemPrompt`/`buildOfferUserPrompt` avec interdiction explicite d'inventer des chiffres — prix absent du brief → chaîne vide affichée « À définir », DEFCON 1 ; `parseOfferContent` défensif : fences markdown, texte parasite, schéma strict ; 10 tests). `createCommercialOfferAction` (Brand DNA via `normalizeBrandDNA` → `getPromptConfig("document_commercial_offer")` fallback ajouté → `callTextLLM` deepseek proxy maxTokens 3000 → persist `content_json`+`brand_dna_snapshot`, statut completed ; échec LLM/parse → erreur honnête, AUCUN document fabriqué) + `duplicateDocumentAction`. Page `/dashboard/documents/[id]` + `CommercialOfferView` (export PDF pattern print-CSS `#offer-doc`). **PURGE MOCK_DOCUMENTS** (4 faux docs) → lecture exclusive table réelle. DocumentsTable : boutons morts câblés (Voir/Télécharger→détail, Dupliquer→action) + libellés FR en dur → i18n existante. Migration `20260315000011_documents.sql` (enums+table+2 index+RLS 4 policies) **appliquée db-rami + RLS testée** (SELECT isolé ✓, INSERT cross-tenant bloqué ✓, rollback). i18n 15 clés × 8 locales (insertion textuelle CRLF). **Browser-verified PROD** (Playwright sur rami.ai-mpower.com, test-ralph) : vide honnête → création (brief Banque Al Yusr, 45 000 MAD) → génération deepseek ~20 s → redirection → TOUTES sections rendues, prix = celui du brief (non inventé) → liste 1 ligne → Dupliquer « (copie) » → suppressions → vide. Cleanup DB vérifié (0 lignes). Gates TS0/lint0/Jest 271/build. *Learnings* : (a) **browser-verify PROD = nouvelle méthode standard** (Playwright MCP direct sur la prod, compte test-ralph, cookies frais — pas de dev local) ; (b) hors-LAN, l'API Coolify ne permet PAS d'exécuter du SQL (pas d'endpoint execute) → migration = SSH on-LAN ou one-liner base64 collé par Amine dans le terminal web Coolify ; (c) DETTE pré-existante détectée et corrigée : 3 liens `/dashboard/billing` → `/billing` (quota-badge + success_url/return_url **Stripe** qui renvoyaient vers une 404 après paiement).
- **2026-06-11 | OPS | Rotation mdp super-admin (à la demande d'Amine)** — Nouveau mdp fort généré localement (jamais affiché), bcrypt $2a$10 calculé en local, UPDATE auth.users + révocation sessions/refresh tokens via SSH, login GoTrue vérifié HTTP 200. **Stocké coffre DPAPI : `RAMI_SUPERADMIN_PASSWORD`** (les sessions futures peuvent le récupérer — plus de rotation nécessaire).
- **2026-06-04 | US-022 | Whisper transcriptions — pipeline RÉEL (impl + pipeline browser-verified ; happy-path bloqué clé)** — Le module était factice (upload non stocké, 0 appel Whisper, liste mock). Rendu réel : `src/lib/services/transcription/whisper.ts` (`transcribeAudio` endpoint `/audio/transcriptions` whisper-1, clé+base configurables `WHISPER_API_KEY`/`WHISPER_BASE_URL`→défaut OPENAI_*/api.openai.com, garde 25 Mo, `toWhisperLanguage` PUR darija→ar ; 6 tests) ; `transcribeUploadAction` (upload MinIO bucket `audios` + entrée DB processing + Whisper + statut completed/failed) ; `TranscriptionUploadZone` (vrai FormData, état honnête, router.refresh) ; `TranscriptionList` **mock supprimé → état vide honnête**. Migration `20260315000010` (enum+table+RLS, **appliquée db-rami**, RLS isolation testée). i18n emptyTitle/emptyDesc + .env WHISPER_* × 8. Gates TS0/lint0/build/jest 261. **Pipeline browser-verified** : upload WAV 31 Ko → MinIO → entrée réelle « rami-test-audio » → Whisper appelé → statut **Échec** « Whisper HTTP 400 » (le proxy LiteLLM ne route pas whisper) → liste rafraîchie, mock disparu. *Learning* : ⚠️ Whisper ≠ proxy LiteLLM (HTTP 400) — exige une VRAIE clé OpenAI (api.openai.com) ou un proxy avec modèle whisper-1 configuré. **passes=false** : happy-path (texte) bloqué sur clé Whisper = input Amine/ops (comme Apollo avant Hunter). US-023/024 (verbatims/résumé, transcription→brief) sont bloquées tant que la transcription ne produit pas de texte.
- **2026-06-04 | DETTE 1 | brand_dna shape PLATE vs NESTÉE (RÉSOLUE, browser-verified)** — `src/lib/services/brand-dna/normalize.ts` : `normalizeBrandDNA` PUR (PLAT→nested) + `causseColorToHex` (ID Causse→HEX via `CAUSSE_COLORS` de brand-dna.schema). Branché dans `visual.actions` (génération `compileBrandDNAToPrompts` + résumé `getTenantBrandDNAAction`) et `workflow.actions` (texte + visuels). 12 tests. **Browser-verified** : /create affichait « Brand DNA non configuré » (bug shape) → désormais « Brand DNA activé pour Banque Test Ralph » + vraies couleurs résolues. *Learning* : couleurs stockées en IDs Causse (`vert_emeraude`), pas HEX → résolution obligatoire ; `hasDNA = !!identity?.name` dépend de brandName.
- **2026-06-04 | DETTE 2 | AiRecommendations hardcodées (RÉSOLUE)** — `src/lib/services/analytics/recommendations.ts` : `buildAiRecommendations` PUR (top par dimension d'attribution → recommandation, ratio→%, tri/limite ; imports TYPE-only → client-safe). `getAiRecommendationsAction` (analytics.ts) : `topFeatures` sur 5 dimensions réelles. `AiRecommendations` refait (props + i18n interpolé + **état vide honnête** si <3 posts/critère ; plus aucune stat inventée). i18n recoBest*/recoEmpty* × 8. 5 tests. Page /analytics rend sans crash. *Learning* : un module pur consommé par un client component doit importer les types d'`attribution.ts` en `import type` (sinon createClient serveur entre dans le bundle).
- **2026-06-02 | GAP enrichissement | Providers PDL + Dropcontact + Enrich.so (BYOK)** — `src/lib/services/leads/{pdl,dropcontact,enrich}.ts` (mapper PUR + `enrichViaX(input,key)`), `types.ts` `EnrichmentProviderId` étendu, `index.ts` routeur `enrichLead` switch + ENV_KEY + `getActiveEnrichmentProvider` validé sur KNOWN_PROVIDERS. Migration `20260315000008` (rows ai_provider_keys, **appliquée db-rami** : 5 providers enrichment). `.env.example` + doc. Tests `tests/unit/enrichment-providers.test.ts` (27). Contrats API vérifiés docs officielles (PDL GET /v5/person/enrich X-Api-Key ; Dropcontact POST async + poll X-Access-Token ; Enrich.so GET /v1/api/person Bearer, mapper défensif car shape non publiée). Gates TS0/lint0/build/jest. *Learning* : Dropcontact = API ASYNCHRONE (submit→request_id→poll) ; `enrichViaDropcontact(input,key,{maxPolls,pollDelayMs})` injectable pour tests rapides.
- **2026-06-02 | Cosmétique | Libellés enrichissement provider-neutres (8 locales)** — i18n `apolloEnrichment/NotEnrichedDesc/EnrichButton/enrichLead` neutralisés (plus de « Apollo ») × 8 locales + messages `leads.actions` (no_key/not_found/error) + commentaire UI. Retiré le faux horodatage `apolloUpdated` « il y a 2h » (DEFCON 1). **Browser-verified** : panneau lead affiche « Enrichissement » / « Enrichir le lead ». Les surfaces de *recherche* Apollo (apolloEngine/poweredByApollo) laissées intactes (feature distincte, hors scope).
- **2026-06-02 | US-028 | Scoring Brand DNA match leads (BROWSER-VERIFIED)** — `src/lib/services/leads/scorer.ts` (PUR : `computeOverall` pondéré 0.35/0.35/0.30, `heuristicLeadScore` repli déterministe, `parseLeadScore`, `assembleLeadScore`, build*Prompt ; 14 tests). `src/lib/services/ai/text-llm.ts` (`callTextLLM` EXTRAIT de workflow.actions → zéro duplication). `scoreLeadAction` (leads.actions) : LLM deepseek → parse → fallback heuristique → persist `brand_dna_match`+`score`. UI bouton « Scorer le match »/« Re-scorer » (LeadDetailPanel) + tri colonnes par score (KanbanBoard `sortByScore`). i18n × 8. prompt-config `leads_brand_dna_scoring`. **Browser-verified on-LAN** (dev→db-rami + tunnel, lead « Banque Al Baraka » finance_islamique/Maroc) : Scorer → **97/100**, breakdown audience/secteur/culture, badge carte 0%→97%, bouton→Re-scorer. *Learning* : brand_dna shape PLATE → `pickString(dna, 'sector','secteur')` (variantes FR tolérées).
- **2026-06-02 | US-014/015 | Rapport Couleur MENA + cadence (BROWSER-VERIFIED)** — `src/lib/services/reports/color-trends.ts` (PUR : `buildColorTrendReport` stance secteur + note culturelle MENA + classement, prompts ; 8 tests) + `color-trends-generator.ts` (I/O : benchmarks service-role + Crawl4AI best-effort + narrative deepseek + upsert). Table `color_trend_reports` + RLS (migration `20260315000009` **appliquée db-rami**, cross-tenant testée : SELECT isolé + WITH CHECK bloqué). Page `/dashboard/color-trends` gatée `performance_loop` (Pro+) + export PDF (print-CSS `@media print`, zéro dep) + nav badge Pro. Worker pg-boss `color-trend-refresh` (cron trimestriel `0 4 1 1,4,7,10 *`) + instrumentation. i18n namespace `colorTrends` × 8 (script). **Browser-verified** : Générer → synthèse deepseek réelle + cartes couleurs (vert/bleu Recommandée en finance_islamique = autorité Causse). *Learning* : PDF = page web + `window.print()` print-only CSS (puppeteer/chromium incompatibles Nixpacks) ; rapport honnête `authority_only` si benchmarks < k=5.
- **2026-06-02 | US-020 | Enforcement quotas génération (BROWSER-VERIFIED)** — `src/lib/billing/usage.ts` (`incrementGenerationCount` atomique reset-aware SQL CASE + PURES `shouldResetGenerations`/`effectiveGenerationCount` ; 6 tests). `getCurrentTenantPlan` : reset PARESSEUX (compteur effectif 0 si période expirée → quota réellement mensuel même si cron en retard). **GAP critique comblé** : `workflow.actions` Step4 `generateVisualContentAction` ajoute checkGenerationQuota + quota_exceeded + incrément sur génération RÉELLE (Fal/Replicate, PAS placeholder). `visual.actions` migré vers le helper (fin course read+1). `Step4VisualGen` carte quota + lien /pricing × 8 locales. **Browser-verified** : generation_count=2000 → /create bloqué + UpgradeModal « 2000/2000 ». *Learning* : ne pas consommer de quota sur le fallback placeholder (DEFCON 1).
- **2026-06-02 | Méthode browser-verify on-LAN (réutilisée, OK)** — tunnel SSH conteneur PG `172.24.0.6:5432` (Git Bash, secrets fetchés inline via `docker exec printenv`, jamais affichés ; longueurs vérifiées) + `npm run dev` override SUPABASE_DB_URL(tunnel)+NEXT_PUBLIC db-rami+anon+service-role+OAUTH+OPENAI. Conteneur app = `ry8ytnene4czxdhsoes0z56y-105027232246`. Playwright MCP (plugin). Cleanup systématique (fixtures restaurées, dev+tunnel tués via Stop-Process port 3000 + ssh -L).
- **2026-06-01 | US-029 | Supprimer les leads de démo (BROWSER-VERIFIED)** — `src/app/(dashboard)/dashboard/leads/page.tsx` : suppression du const `DEMO_LEADS` (≈215 lignes de faux prospects) + de la logique de fallback démo ; remplacé par `EMPTY_LEADS` (toutes colonnes vides) → la page lit UNIQUEMENT la vraie table `leads` (`getLeadsAction`), état vide honnête si aucun lead (jamais de données fabriquées) ; en cas d'échec de chargement → vide (pas de démo). Gates TS0/lint0/build OK. **Browser-verified on-LAN** (dev→db-rami + tunnel, tenant test 0 lead) : 4 colonnes à **0 / 0 MAD / « Aucun lead »** + bouton Ajouter ; **plus aucun lead de démo** (TechMoove/LogiStream/DataPulse disparus). *Learnings* : KanbanBoard a déjà un état vide par colonne (« Aucun lead ») ; les « TechMoove SAS » restants sont des **placeholders de formulaire** (AddLeadDialog), pas des données affichées → laissés. Débloqué par la migration CRM (table leads existe désormais).
- **2026-06-01 | US-027 | Enrichissement Apollo (impl + gates ; BROWSER-VERIFY DIFFÉRÉ)** — Service `src/lib/services/leads/apollo.ts` : `mapApolloPerson(person)` **PUR** (title/headline fallback, organization{.name|organization_name}, email, linkedin_url, industry, estimated_num_employees→string, location=city/state/country joints, `raw` traçable) + `enrichViaApollo(input)` (POST `https://api.apollo.io/api/v1/people/match`, header `X-Api-Key`, `AbortSignal.timeout(15s)` ; dégradation propre : `no_key` si `APOLLO_API_KEY` absente, `not_found` si aucun critère/aucun person, `error` sur HTTP non-ok/exception). Action `enrichLeadAction(leadId)` dans `leads.actions.ts` (charge le lead tenant-scoped → enrichit → **complète uniquement les champs vides** email/linkedin/sector/company_size/location, n'écrase jamais une saisie + `apollo_data`=enrichment ; messages FR par reason ; log error sur reason 'error'). UI : bouton « Enrichir/Réenrichir via Apollo » + spinner + erreur inline dans le bloc Apollo de `LeadDetailPanel.tsx` (prop `onEnriched`), câblé dans `KanbanBoard.tsx` (`handleLeadEnriched` met à jour board + selectedLead). i18n `apolloEnrichButton`/`apolloReEnrich`/`apolloEnriching` × **8 locales**. Tests Jest `tests/unit/apollo-leads.test.ts` (10 : mapping complet/fallback/minimal/location partielle/size non-num ; enrich no_key/no-criteria/not_found/success/HTTP-error avec `global.fetch` mocké). Gates **TS0 / lint0 / build OK / jest (10 + suite existante)**. `APOLLO_API_KEY` déjà dans `.env.example`. *Learnings* : la colonne `leads.apollo_data` (jsonb) + le bloc UI Apollo **existaient déjà** (statiques) → US-027 = les rendre fonctionnels ; Apollo People Match v1 = `X-Api-Key` + body `{name,email,organization_name,linkedin_url}` → `{person:{...}}` ; mapping testé sans réseau (pas d'appel MCP Apollo pour éviter coût/PII). **⛔ BROWSER-VERIFY DIFFÉRÉ** : nécessite `APOLLO_API_KEY` (env Coolify/serveur) + un lead réel sur db-rami + login test → bloqué hors-LAN cette session. **passes reste false.**
- **2026-06-01 | US-021 | Événements Sentry/PostHog réels (COMPLET, passes=true)** — Audit : les 5 événements PostHog requis étaient **déjà câblés** (`tenant_signup` auth.actions.ts:62, `brand_dna_completed` brand-dna.actions.ts:74, `visual_generated` visual.actions.ts:368, `post_published`/`post_failed` publish-worker.ts:202, `subscription_upgraded`/`cancelled` stripe webhook:73) via `captureServerEvent` (`src/lib/utils/posthog-server.ts`). **0 `console.log`** dans `src` (logger structuré `src/lib/utils/logger.ts` qui route `error`/`critical` → `Sentry.captureMessage` en prod uniquement). Sentry déjà couvert sur **publishing** (publish-worker catches niveau error) et **billing** (stripe-webhook: invalid_signature/processing_error/db_update_failed). **Gap comblé = génération** : `visual.actions.ts` (1) `persistVisualSession` catch L119 avalait l'erreur silencieusement → ajout `log({level:'error', action:'persist_session_failed'})` ; (2) échec « aucune image générée » (tous providers/directions échouent) → ajout `log({level:'error', action:'generation_failed', metadata:{errors}})`. `workflow.actions.ts` loggait déjà ses erreurs (L256/L451). Gates **TS0 / lint0 / build OK / jest 163/163**. **Pas de browser-verify requis** (aucune UI nouvelle) ni de table → story complétable intégralement hors-LAN. *Learnings* : le routage Sentry passe par `logger.log({level:'error'|'critical'})` (pas d'appels `Sentry.captureException` directs hors error boundaries `error.tsx`) ; en DEV le logger écrit en console (normal, non concerné par « pas de console.log en PROD ») ; quota génération (`checkGenerationQuota`/`getPlanConfig`/`quota_exceeded`) déjà présent dans visual.actions.ts → US-020 partiellement amorcée.
- **2026-05-31 | US-001 | Schéma post_metrics + RLS** — `src/lib/db/schema.ts` (table `postMetrics` + types `PostMetric`/`NewPostMetric`, import `real`), `supabase/migrations/20260315000001_post_metrics.sql` (table + 4 policies RLS `tenant_id = get_current_tenant_id()` + 2 index). Appliqué sur db-rami (RLS=t, 4 policies). Gates: TS0/lint0/build OK. *Learning* : schéma réel = `src/lib/db/schema.ts` (pas packages/db) ; migrations dans `supabase/migrations/` ; enum `platform` réutilisé.
- **2026-05-31 | US-002 | MetricsProvider + collecte X (Twitter)** — `src/lib/services/metrics/types.ts` (`NormalizedMetrics`, `MetricsFetchInput`, `MetricsResult` union avec `unavailable`, interface `MetricsProvider`), `engagement.ts` (`computeEngagementRate` borné [0,1]), `twitter.ts` (`fetchTwitterMetrics` → API v2 `GET /2/tweets/:id?tweet.fields=public_metrics,non_public_metrics,organic_metrics`, 401/403/404 → `{unavailable:true}` sans crash), `index.ts` (router `collectMetricsFromPlatform` calqué sur `publishing/index.ts`). Gates: TS0/lint0/build OK. *Learnings* : table OAuth réelle = `oauth_connections` (PAS `social_accounts` du schéma Drizzle, vestige) ; `posts.platform_results` JSONB = `{ twitter: { postId, postUrl, status }, ... }` → source du `platformPostId` pour la collecte ; mapping Twitter→post_metrics : shares = retweet+quote, saves = bookmark_count, clicks = non_public.url_link_clicks. `AbortSignal.timeout(15000)` pour le réseau.
- **2026-05-31 | US-003 | Collecte metrics LinkedIn** — `src/lib/services/metrics/linkedin.ts` (`fetchLinkedInMetrics`) + branché dans le router. Deux chemins selon l'author URN (`accountId`) : organisation (`urn:li:organization:`) → `organizationalEntityShareStatistics` (impressions/likes/comments/shares/clicks réels) ; membre (`urn:li:person:`) → `socialActions/{urn}` (likes+comments uniquement). 401/403 → `{unavailable:true}` (scope absent géré gracieusement). Header `X-Restli-Protocol-Version: 2.0.0`. Gates: TS0/lint0/build OK. *Learning* : impressions d'un post de MEMBRE non exposées par l'API LinkedIn → laissées à 0 (donnée honnête, jamais estimée — DÉFCON 1) ; `platformPostId` LinkedIn = le share/ugcPost URN ; `accountId` = author URN.
- **2026-05-31 | US-004 | Collecte metrics Meta (FB+IG)** — `src/lib/services/metrics/meta.ts` : `fetchFacebookMetrics` (Graph `?fields=likes.summary(true),comments.summary(true),shares,insights.metric(post_impressions,post_impressions_unique,post_clicks)`) + `fetchInstagramMetrics` (2 appels : `?fields=like_count,comments_count` puis `/insights?metric=reach,saved,shares,impressions`). Deux fonctions branchées séparément (`facebook`/`instagram`) dans le router. Token Meta en query param ; 401/403/code 190 → `{unavailable:true}` ; insights IG en échec → dégradation propre (garde likes/comments). Gates: TS0/lint0/build OK. *Learnings* : FB `platformPostId` = `{page}_{post}`, IG = media id ; FB pas de `save` (saves=0), IG pas de `clicks` (clicks=0) ; reach stocké dans `raw` (pas de colonne post_metrics) ; impressions IG fallback = reach si `impressions` déprécié.
- **2026-05-31 | US-005 | Collecte metrics Pinterest** — `src/lib/services/metrics/pinterest.ts` (`fetchPinterestMetrics`) + branché. `GET /v5/pins/{id}/analytics?start_date&end_date&metric_types=IMPRESSION,SAVE,PIN_CLICK,OUTBOUND_CLICK` (fenêtre 30 j obligatoire, max 90). Parsing défensif `summary_metrics` (racine ou `all.summary_metrics`). clicks = PIN_CLICK+OUTBOUND_CLICK ; likes/comments/shares = 0 (Pinterest n'a pas ces metrics). 401/403/404 → `{unavailable:true}`. **Router metrics complet (5 plateformes Phase 1)** : twitter/linkedin/facebook/instagram/pinterest. Gates: TS0/lint0/build OK. *Learning* : Pinterest analytics EXIGE start_date/end_date ; `new Date()` OK en code runtime (interdiction Date.now() concerne uniquement les scripts Workflow).
- **2026-05-31 | US-006 | Job pg-boss collecte planifiée** — `src/lib/queue/token-refresh.ts` (extraction DRY de `getValidAccessToken` depuis publish-worker, partagé par les 2 workers), `src/lib/queue/jobs/collect-metrics.ts` (`startCollectMetricsWorker` + `processCollectMetricsJob` : lit `posts.platform_results` → pour chaque plateforme publiée, conn OAuth active → token → `collectMetricsFromPlatform` → INSERT snapshot `post_metrics`), `pgboss.ts` (job `COLLECT_METRICS` + `CollectMetricsPayload{postId,tenantId,window}` + `scheduleMetricsCollection` T+1h/24h/7j, retry x3 backoff, singletonKey `metrics:{post}:{window}`), `instrumentation.ts` (démarre le worker), publish-worker (étape 8 : `scheduleMetricsCollection` best-effort après `published`). Gates: TS0/lint0/build OK. *Learnings* : `platformPostId` source = `posts.platform_results[platform].postId` (PublishResult) ; collecte = INSERT snapshot horodaté (série temporelle, pas écrasement) ; `unavailable` (token révoqué) → skip sans retry, seules les erreurs DB transitoires throw pour déclencher le retry pg-boss ; `boss.send(startAfter: seconds)` accepte un nombre de secondes.
- **2026-05-31 | US-007 | Attribution feature→performance** — migration `supabase/migrations/20260315000002_attribution.sql` (**appliquée sur db-rami**, RLS testée cross-tenant ✅ : SELECT isolé + INSERT WITH CHECK bloqué) : VIEW `attribution_facts` `security_invoker=true` (dernier snapshot par post×plateforme + dimensions `ai_metadata`) + table `attribution_rankings` (4 policies RLS). `src/lib/db/schema.ts` (+`attributionRankings`, import `bigint`). `src/lib/services/metrics/attribution.ts` (`aggregateAttribution` pur + `topFeatures(tenantId,sector,dimension)` live, `MIN_SAMPLE_SIZE=3`). `src/lib/queue/jobs/attribution-refresh.ts` (cron 6h incrémental → recalcule cache via service client, purge+insert par tenant). pgboss `ATTRIBUTION_REFRESH` + instrumentation. Gates: TS0/lint0/build OK + RLS db. *Learnings* : `generated_assets` n'existe pas → dimensions dans `posts.ai_metadata` (NULL honnête si absent) ; vue `security_invoker` hérite RLS sans policy ; `NewAttributionRanking.computed_at` = Date (mode Drizzle) → laisser défaut DB `now()` ; lien post→`visual_session_images` flou (URL, pas FK) → non utilisé MVP.
- **2026-05-31 | US-008 | Performance prior dans Prompt Compiler** — `prompt-compiler.ts` (type `PerformancePrior` + 5e param `performancePrior`, couleurs gagnantes en tête de `effectiveColors` + `performanceKW` style gagnant injecté au prompt positif). `attribution.ts` (`buildPerformancePrior(tenantId,sector)` via `topFeatures` couleurs+styles, gaté `MIN_PRIOR_SAMPLE=10` → `null` sinon = Causse pur, FR-2). `visual.actions.ts` (construit le prior, le passe au compiler + à `persistVisualSession`). Migration `20260315000003_performance_prior.sql` (colonne `visual_session_images.performance_prior` jsonb, **appliquée db-rami**). Gates: TS0/lint0/build OK. *Learnings* : `PerformancePrior` défini dans le compiler (pur) pour éviter un cycle (attribution importe le TYPE) ; ajout de colonne sur table RLS existante → hérite la RLS, pas de test cross-tenant séparé.
- **2026-06-01 | US-013 | Page publique Causse×Culture (MOAT-2, BROWSER-VERIFIED)** — Route publique `src/app/(public)/causse/page.tsx` (1er `(public)` route group du projet ; pas de layout dédié requis, le root layout fournit html/providers). Server Component rendant les 8 couleurs de `causse-matrix.ts` (nom, HEX + variantes, émotions en badges, effet physio, réseaux, secteurs reco/à éviter, notes culturelles Maroc/Afrique/Europe/Moyen-Orient, citation Causse). **SEO complet** : `generateMetadata` (title/description/keywords/canonical/robots index) + **OpenGraph** (article) + **Twitter card** + **JSON-LD Schema.org** `DefinedTermSet`/8×`DefinedTerm` (échappement `<`→`<` anti-`</script>`) + entrée **sitemap.ts** (priority 0.8). **i18n** namespace `causse` (18 clés) ajoutée aux **8 locales** via script node (insertion textuelle après `{` sans reformater). Design dark RAMI (blobs + grid). Gates TS0/lint0/build OK. **Browser-verified** (dev override NEXT_PUBLIC→db-rami, PAS de tunnel — page sans Drizzle) : accès **anonyme** (cookies cleared, pas de redirect login), h1 + 8 cartes, JSON-LD DefinedTermSet 8 termes, og:title + canonical + meta desc présents, **switch FR↔EN OK** (title/badge/labels). *Learnings* : route group `(public)` non listé dans `proxy.ts` protectedPaths → public d'office ; le proxy appelle `getUser()` sur CHAQUE requête → même une page publique exige un `NEXT_PUBLIC_SUPABASE_URL` joignable (override db-rami) sinon 500 ; ⚠️ tuer le dev zombie sur :3000 entre 2 runs (`Get-NetTCPConnection -LocalPort 3000 | Stop-Process`) sinon EADDRINUSE ; `.next/dev/types/validator.ts` stale après ajout de route → `rm -rf .next/dev` + build régénère.
- **2026-05-31 | US-012 | Dashboard analytics réel (impl + tests, BROWSER-VERIFY BLOQUÉ)** — Remplacé tout le **factice** (`Math.random`, `simulateEngagement`/`simulateReach`, DEMO_POSTS, `reach*3.87`, `score*35`, fallbacks `||1_200_000`/`5.8`/`12`, follower growth sans source) par du **réel** issu de `post_metrics`. Logique extraite en module PUR `src/lib/services/analytics/aggregate.ts` (`assembleAnalytics` + `sumMetrics`/`pctDelta`/`round1`) ; l'action `app/actions/analytics.ts` ne fait plus que l'I/O Drizzle (dernier snapshot par post×plateforme) + délègue. KPIs réels : impressions, interactions (likes+comments+shares+saves), taux engagement (interactions/impressions), clics, likes + deltas vs période précédente. Composants : `kpi-cards.tsx` (5 cartes réelles, clés i18n existantes `impressions`/`engagement`/`engagementRate`/`clicks`/`likes` — **zéro ajout i18n**), `top-posts-table.tsx` (vraies reach/engagementRate/clics/interactions par post + empty state), `analytics-dashboard.tsx` (score perf sur données réelles + **retrait du bloc « phase2Notice » devenu mensonger**). Tests Jest `tests/unit/analytics-aggregate.test.ts` (11/11) : KPIs réels, **pas de metrics → tous KPIs à 0 / topPosts metrics 0 (zéro fabrication)**, deltas, ranking top posts, daily par plateforme. Gates TS0/lint0/build OK + jest 21/21 (mes 2 suites). **✗ BROWSER-VERIFY BLOQUÉ** : l'action analytics utilise **Drizzle (`SUPABASE_DB_URL` Postgres direct)**, pas le client Supabase HTTPS. db-rami Postgres = **interne** (cloudflared tunnel = HTTPS only, port PG non exposé) → la méthode US-011 (override `NEXT_PUBLIC_*`) NE marche PAS (Drizzle taperait l'instance cloud `rfndj...`). `.env.local` : `SUPABASE_DB_URL`+`NEXT_PUBLIC_SUPABASE_URL` = instance **cloud** `rfndjbrwpdltfzvyreyv` (cohérente entre elles, mais cloud non migré Performance Loop). **RÉSOLU → BROWSER-VERIFIED `passes=true`.** Décision Amine : utiliser le Supabase **déjà sur Coolify** (db-rami), pas de stack local. ⚠️ L'instance cloud dev de `.env.local` (`rfndjbrwpdltfzvyreyv`) est **SUPPRIMÉE** (DNS NXDOMAIN API+DB) — `.env.local` pointe vers un projet mort. **Méthode validée (réutilisable pour toute story Drizzle)** : (1) **tunnel SSH** vers le Postgres BRUT de db-rami — ⚠️ le `127.0.0.1:5432` du serveur = **supavisor pooler** (erreur « Tenant or user not found »), il faut cibler le **conteneur** : `ssh -i $key -N -L 54322:172.24.0.6:5432 $u@$h` (172.24.0.6 = IP bridge du conteneur `supabase-db-szn6...`, via `docker inspect`). Mot de passe PG = `docker exec <db> printenv POSTGRES_PASSWORD` (récupéré côté serveur, jamais affiché). (2) `npm run dev` avec override : `SUPABASE_DB_URL=postgresql://postgres:<pwd>@127.0.0.1:54322/postgres` (Drizzle via tunnel) + `NEXT_PUBLIC_SUPABASE_URL=https://db-rami.ai-mpower.com` + anon db-rami (auth HTTPS, comme US-011). (3) seed 3 posts publiés + 4 `post_metrics` pour le tenant test (UUID fixes `...1111111110[123]` pour cleanup) sur db-rami via SSH psql. (4) Playwright `/dashboard/analytics`. **Résultats EXACTS** : Impressions 18.0k (18000), Engagement 1.2k (1248), Taux 6.9% EXCELLENT, Clics 142, J'aime 970, donut IG 60%/LI 20%/X 19%, top posts triés par interactions réelles (#1 Étude IG 8000/9.4%/0clic, #2 Lancement LI 8000/5.4%/130, #3 Astuce X 2000/3.2%/12), bloc phase2Notice supprimé. **Tout = seed réel, zéro fabrication.** (5) **cleanup** : seed supprimé (0 posts/0 metrics restants), tunnel fermé. *Learnings* : tunnel doit viser le conteneur PG (pas le pooler host:5432) ; pg-boss du dev local se connecte alors à db-rami via le tunnel (workers concurrents avec la prod — OK sur fenêtre courte, arrêter le dev vite) ; ⚠️ `.env.local` = projet cloud mort, ne JAMAIS lancer `npm run dev` SANS override (DNS échoue).
- **⚠️ DETTE PRÉ-EXISTANTE détectée (hors-scope Ralph)** : `tests/unit/brand-dna-score.test.ts` échoue sur HEAD (avant mes modifs) — `SECTOR_COLOR_RULES` secteur `pharmacie_parapharmacie` : `avoidReasonKey` manquante alors qu'`avoidAlternative` est définie. À corriger dans une story dédiée (je n'ai pas touché brand-dna.schema). **→ RÉSOLU en US-016** (`avoidReasonKey: "sante_medical"` ajoutée, réutilise l'i18n existant ; brand-dna-score.test.ts repasse).
- **2026-06-01 | US-016 | Score cohérence culturelle (impl + gates ; BROWSER-VERIFY DIFFÉRÉ)** — Module PUR `src/lib/services/brand-dna/cultural-scorer.ts` : `nearestCausseColor(hex)→id` (plus proche voisin RGB, distance euclidienne sur les 12 `CAUSSE_COLORS`) + `getCulturalLevel(score)` + `scoreCulturalCoherence({sector, colorIds})→{score 0-100, level, justifications[], hasRules}` (barème : baseline 70, +10 par couleur recommandée, −20 par couleur à éviter, clamp [0,100] ; niveaux excellent≥85/good≥65/warning≥45/poor ; secteur hors `SECTOR_COLOR_RULES` → `hasRules:false` score neutre 70) + variante `scoreCulturalCoherenceFromHex({sector, hexColors})` (visuels). **FIX dette** : `pharmacie_parapharmacie.avoidReasonKey = "sante_medical"` (`src/lib/schemas/brand-dna.schema.ts`). Badge **Server Component async** `src/components/brand-dna/cultural-score-badge.tsx` (résout i18n via `getTranslations` : titre/niveau/raison `sectorColorRules`/nom couleur/alternative) intégré dans la page `dashboard/brand-dna/page.tsx` (Section 2 Palette, rendu si `_paletteColors.length>0`). i18n namespace `brandDna.culturalScore` (title, levels.{excellent,good,warning,poor}, recommended/neutral/avoid/alternative {color}/noRules) ajouté aux **8 locales** (script node textuel, ancre `"scoreLevel"`, JSON revalidé). Tests Jest `tests/unit/cultural-scorer.test.ts` (24 tests : nearest exact/proche/invalide, bornes niveaux, reco/avoid/neutre, raison+alternative finance_islamique & sante_medical & pharmacie, secteur sans règle, clamp 0/100, mixte, ids inconnus filtrés, fromHex). Gates **TS0 / lint0 / build OK / jest 90/90** (cultural-scorer 24 + brand-dna-score 66, dette incluse). *Learnings* : le score culturel est un attribut de la PALETTE (couleurs Causse choisies × secteur), PAS d'un visuel individuel (`VisualDirection.color_emphasis` est un texte descriptif, pas du HEX) → ancré sur la page Brand DNA, pas VisualCard ; `getBrandDnaAction()` lit via Supabase HTTPS (`createClient()`), donc le browser-verify de cette page NE nécessite PAS le tunnel Postgres (contrairement au dashboard analytics US-012). **⛔ BROWSER-VERIFY BLOQUÉ cette session** : SSH/LAN injoignable (192.168.100.8 ET .3 timeout — poste hors réseau local serveur), Coolify API masque les valeurs d'env (endpoint `/envs` ne renvoie pas `value`), bundle prod n'expose pas la config Supabase dans les chunks de /login. Décision Amine (2026-06-01) : commit impl + checkpoint, browser-verify reporté en session on-LAN. **passes reste false.**
- **2026-05-31 | US-010 | Job agrégation collective (MOAT-1)** — Module PUR `src/lib/services/metrics/collective.ts` (`computeTenantPlatformStats` moyennes/plateforme d'un tenant ; `aggregateCollective` k-anonymity ≥ `K_ANONYMITY_MIN=5` → 2 metrics `avg_engagement`/`avg_impressions` par bucket qualifiant ; `benchmarkKey` clé canonique JSON.stringify anti-collision). Worker `src/lib/queue/jobs/collective-aggregate.ts` (`startCollectiveAggregateWorker` cron quotidien `0 3 * * *` ; `aggregateCollectiveBenchmarks` : tenants `collective_optin=true` + secteur/culture lus `brand_dna->>'sector'`/`->>'primaryCulture'` → dernier snapshot post×plateforme `post_metrics` → `computeTenantPlatformStats` → `aggregateCollective` → **upsert** `onConflict:'sector,culture,platform,metric'` + **pruneStale** des agrégats devenus non-qualifiants = table toujours véridique). Service client (bypass RLS). Enregistré `JOBS.COLLECTIVE_AGGREGATE` (pgboss.ts) + `instrumentation.ts`. Test Jest `tests/unit/collective-aggregate.test.ts` (10/10) : **<5 tenants → 0 ligne** ✓, =5 → 2 metrics, **moyenne des moyennes-tenant** (gros tenant ne domine pas) ✓, dédup tenant/bucket ✓, buckets indépendants ✓. Gates TS0/lint0/build OK + jest 10/10. *Learnings* : runner = **Jest** (`npx jest <pattern>`), pas Vitest ; agrégation = moyenne des moyennes-tenant (chaque marque pèse pareil) ; upsert PostgREST `onConflict` cible l'index unique `uq_collective_benchmarks_dims` ; le filtre k≥5 s'applique AVANT l'insert (le CHECK SQL US-009 = dernier garde-fou) ; prod n'a que 2 tenants → le vrai job produit 0 ligne (correct), validation = unitaire (le service client local pointe l'instance cloud via .env.local, pas db-rami).
- **2026-05-31 | US-011 | Opt-in RGPD — BROWSER-VERIFIED** — Vérif navigateur exécutée (dernier gate manquant) → `passes=true`. **Méthode compte test db-rami** (db-rami n'avait que le super-admin) : création du compte 100% SQL (PAS l'API admin GoTrue, pas de service-role en clair) — CTE `auth.users`(bcrypt via `crypt(pwd, gen_salt('bf'))`)+`auth.identities`(⚠️ colonne `email` GÉNÉRÉE → ne pas l'insérer ; `provider_id`=user_id::text)+`public.tenants`(brand_dna `{"sector":"tech","primaryCulture":"maroc"}`, collective_optin=false)+`public.users`(role `agency_owner`). **2 gotchas GoTrue** : (1) colonnes token (`confirmation_token`, `recovery_token`, `email_change*`, `phone_change*`, `reauthentication_token`) NULL → GoTrue `Scan error converting NULL to string` (HTTP 500 "Database error querying schema") → `UPDATE ... SET col=COALESCE(col,'')`. (2) le **proxy lit `onboarding_completed` depuis `user_metadata` (JWT/`raw_user_meta_data`)**, PAS la colonne `public.users` → sinon boucle redirect `/settings`→`/onboarding` → `UPDATE auth.users SET raw_user_meta_data=jsonb_build_object('onboarding_completed',true,...)` + **re-login** (JWT frais). Dev local override env (sans toucher .env.local) : `$env:NEXT_PUBLIC_SUPABASE_URL='https://db-rami.ai-mpower.com'; $env:NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon db-rami>` (Next n'écrase pas un env déjà défini par `.env.local`). Toggle `sr-only peer` (div stylé intercepte le clic) → Playwright `locator('aria-ref=eXXX').click({force:true})`. **Résultats** : section « Intelligence collective » rendue (texte consentement complet : anonymisation + k≥5 + finalité + retrait), OFF→ON → DB `collective_optin=t`, reload → reste ON (persistance `getCollectiveOptinAction`), ON→OFF → DB `f`. Gates TS0/lint0. **Compte test réutilisable** (US-012/031/032) : `test-ralph@rami.local` / `TestRalph2026!`, tenant slug `test-ralph` (id à requêter), user id `f73ccdad-5b6b-4f40-a905-ab3737fe2c18`.
- **2026-05-31 | US-009 | Schéma collective_benchmarks + anonymisation** — migration `20260315000004_collective_benchmarks.sql` (**appliquée db-rami**, **3 tests sécurité passés** : isolation secteur ✅, insert authenticated bloqué ✅, k-anonymity CHECK ✅). Helper `get_current_tenant_sector()` (SECURITY DEFINER, `brand_dna->>'sector'`). Table `collective_benchmarks(sector,culture,platform,metric,value,sample_size,updated_at)` SANS tenant_id (cross-tenant) + `CONSTRAINT CHECK(sample_size>=5)` + unique `(sector,culture,platform,metric)`. RLS : 1 seule policy SELECT `sector=get_current_tenant_sector()` ; aucune policy write → authenticated bloqué, service_role bypass. `src/lib/db/schema.ts` (+`collectiveBenchmarks`). Gates: TS0/lint0/build OK. *Learnings* : secteur réel = `brand_dna->>'sector'` (PLAT) ; service_role bypass RLS confirmé (write ok sans policy) ; CHECK k-anonymity = garde-fou structurel en plus du filtre job.
