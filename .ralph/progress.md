# Ralph Progress — RAMI Complétion

## Codebase Patterns (à suivre)
- **Stack** : Next.js 16 (App Router, Turbopack), React 19 (React Compiler), TS strict, Tailwind v4, shadcn base-nova (@base-ui/react), Zod v4.
- **Proxy middleware** : `src/proxy.ts` (export `proxy()`), pas `middleware.ts`.
- **Supabase** : clients `src/lib/supabase/{server,client}.ts` ; service role `src/lib/supabase/service.ts` (bypass RLS). Auth SSR.
- **DB** : Drizzle (`packages/db/schema/*` ou `src/lib/db/`), migrations SQL `supabase/migrations/` (préfixe timestamp `2026...000NNN_`). Instance dédiée `supabase-db-szn6rjsrqig7n4oerw27egwr` (db-rami.ai-mpower.com).
- **RLS** : OBLIGATOIRE sur chaque table. `get_current_tenant_id()` lit `public.users.tenant_id`. Policy type `tenant_id = get_current_tenant_id()`.
- **Tenant resolution** : `resolveUserTenant(supabase, userId)` (tenants.owner_id → tenant_members → users.tenant_id).
- **LLM** : via proxy LiteLLM (`OPENAI_BASE_URL=https://proxy.ai-mpower.com/v1`, `OPENAI_API_KEY`). Texte = `deepseek-v4-flash` (`LLM_TEXT_MODEL`), vision = `moonshot-v1-8k-vision-preview` (`VISION_MODEL`, BASE64 only). Provider "openai" route vers le proxy. Config par cas d'usage : `src/lib/services/ai/prompt-config.ts` (DB `ai_prompts_config` + fallback).
- **Crawl4AI** : `CRAWL4AI_BASE_URL=https://crawl4ai.ai-mpower.com`, `POST /md {url, filter:"fit"}` → markdown.
- **Abstractions** : `PublishingProvider`, `ImageProvider` (5 providers fallback), `MetricsProvider` (`src/lib/services/metrics/` — types.ts + engagement.ts + index.ts router `collectMetricsFromPlatform`). OAuth refresh : `getValidToken()`.
- **Queue** : pg-boss connecté (schéma `pgboss`), jobs enregistrés dans `src/instrumentation.ts`. Jobs : `PUBLISH_POST` (publish-worker), `COLLECT_METRICS` (jobs/collect-metrics). Refresh OAuth partagé : `src/lib/queue/token-refresh.ts` (`getValidAccessToken(conn, supabaseService)`). `scheduleMetricsCollection({postId,tenantId})` planifie T+1h/24h/7j via `boss.send(..., {startAfter: seconds, singletonKey})`. Cron : `ATTRIBUTION_REFRESH` (jobs/attribution-refresh, `boss.createQueue`+`boss.work`+`boss.schedule('0 */6 * * *')`).
- **Attribution** : vue `attribution_facts` (security_invoker, lecture live RLS) + table cache `attribution_rankings` (RLS tenant). Service `src/lib/services/metrics/attribution.ts` (`topFeatures` live + `aggregateAttribution` pur). Dimensions extraites de `posts.ai_metadata` (`cognitive_objective`, `direction`, `dominant_color_hex`, `hook`, `format`, `brand_dna_score`) + heure (`scheduled_at`/`published_at`) + `platform`. `buildPerformancePrior(tenantId, sector)` → `PerformancePrior|null` (gaté `MIN_PRIOR_SAMPLE=10`).
- **Génération** : `compileBrandDNAToPrompts(brandDNA, brief, platform, seed, performancePrior?)` — prior pondère les couleurs (gagnantes en tête) + emphase style. `visual_session_images.performance_prior` (jsonb) persiste le prior. Table images = `visual_session_images` (PAS dans schema.ts Drizzle, SQL-only).
- **Storage** : MinIO (privé) + R2 (CDN). `src/lib/services/storage/`.
- **Crypto** : OAuth tokens AES-256-GCM (`OAUTH_TOKEN_ENCRYPTION_KEY`).
- **Conventions** : français accentué (commentaires/UI), `useWatch()` (pas `watch()`), Zod v4 `defaultValues` dans useForm (pas `.default()` avec zodResolver), `useCallback` retiré si React Compiler le gère, pas de composant créé en render.

## Pièges connus
- `output: "standalone"` conditionnel `BUILD_STANDALONE` (sinon casse `next start` Nixpacks).
- Moonshot vision : base64 inline uniquement (fetch+encode les URLs).
- deepseek : pas via `/v1/messages` (Anthropic) — uniquement `/v1/chat/completions`.
- Un compte créé hors onboarding n'a pas de tenant → 404 (créer tenants + users row).

## Déploiement
- Coolify app `rami` (uuid ry8ytnene4czxdhsoes0z56y), Nixpacks, auto-deploy sur push. API Coolify : token coffre.
- cloudflared tunnel nahda ; DNS via `cloudflared tunnel route dns --overwrite-dns 7156c3f9-... <host>`.

## CHECKPOINT — Prochaine story : US-010 (Job d'agrégation collective)
*Posé le 2026-05-31 après US-009. deps US-009 ✓ + US-011 (consentement opt-in) — US-011 est requise.*

**Ordre conseillé : US-011 (consentement opt-in RGPD) AVANT US-010** (US-010 ne doit agréger que les tenants consentants).
- **US-011** : flag de consentement par tenant (ex. colonne `tenants.collective_opt_in boolean default false` OU table dédiée) + UI réglages pour l'activer (vérif navigateur). RGPD : opt-in explicite, désactivable.
- **US-010** : job pg-boss `collective.aggregate` (quotidien, cron `boss.schedule`). Agrège `post_metrics` (dernier snapshot par post×plateforme, via le même pattern que attribution) par (secteur, culture, plateforme, metric) — secteur/culture lus depuis `tenants.brand_dna` (shape PLAT : `->>'sector'`, `->>'primaryCulture'`). N'inclut QUE les tenants opt-in (US-011) ET ne produit une ligne QUE si ≥ 5 tenants distincts (le CHECK k-anonymity bloquera sinon → filtrer AVANT insert). Upsert sur `(sector,culture,platform,metric)`. Test : <5 tenants → 0 ligne. Service client (bypass RLS).
  - metrics à produire : `avg_engagement`, `avg_impressions` (moyennes par tenant puis moyenne des tenants, ou moyenne globale — choisir moyenne des moyennes-tenant pour éviter qu'un gros tenant domine). `sample_size` = nb de tenants distincts.

## ⚠️ SPLIT ENVIRONNEMENT dev/prod (découvert US-011) — CRITIQUE
- `.env.local` (dev local) → `NEXT_PUBLIC_SUPABASE_URL=https://rfndjbrwpdltfzvyreyv.supabase.co` (instance Supabase **cloud hébergée**, séparée).
- **Prod** = `db-rami` (Coolify, conteneur `supabase-db-szn6...`) où j'applique les migrations via SSH.
- **Conséquence** : les migrations appliquées sur db-rami NE sont PAS sur l'instance dev cloud, et inversement. Un `npm run dev` local tape sur rfndjbrwpdltfzvyreyv (où `collective_optin` n'existe pas encore). db-rami n'a QUE le super-admin (mot de passe non accessible — anti-leak). → **Vérification navigateur authentifiée non triviale** : nécessite soit appliquer les migrations aussi sur l'instance dev + un compte test connu, soit un déploiement preview, soit un magic-link via service-role.
- **DÉCISIONS Amine (2026-05-31)** : (1) migrations sur **db-rami uniquement** (prod = source de vérité, ne PAS synchroniser l'instance cloud). (2) browser-verify via **compte test + dev local pointé sur db-rami**.

## CHECKPOINT — Vérif navigateur US-011 (à exécuter en session fraîche)
US-011 = **implémenté + RLS testée db-rami** mais `passes=false` tant que la vérif navigateur n'est pas faite. Plan d'exécution :
1. **Créer un compte test sur db-rami** (n'a que le super-admin). Via l'API admin GoTrue (service-role) — récupérer la SERVICE_ROLE_KEY depuis l'env du conteneur `rami` côté serveur SANS l'afficher (tout en un seul SSH), POST `http://supabase-kong-szn6...:8000/auth/v1/admin/users` `{email:"test-ralph@rami.local", password:<connu, ex 'TestRalph2026!'>, email_confirm:true}`. Puis SQL : `INSERT public.tenants(slug,name,owner_id,plan)` + `INSERT public.users(id,email,tenant_id,role,onboarding_completed)` + `UPDATE tenants SET brand_dna='{"sector":"tech"}'::jsonb` (sinon 404 tenant, cf. GOTCHA). Imprimer uniquement l'user id.
2. **Dev local → db-rami** : lancer `npm run dev` avec override env (NE PAS modifier .env.local) : `NEXT_PUBLIC_SUPABASE_URL=https://db-rami.ai-mpower.com` + anon key db-rami (à récupérer depuis l'env conteneur rami, non-secret). En PowerShell : `$env:NEXT_PUBLIC_SUPABASE_URL=...; $env:NEXT_PUBLIC_SUPABASE_ANON_KEY=...; npm run dev`.
3. **Playwright** (MCP) : login test-ralph → `/settings/profile` → vérifier la section « Intelligence collective » + screenshot → toggle ON → recharger → confirmer persistance (DB `tenants.collective_optin=true`) → toggle OFF.
4. Nettoyer le compte test si souhaité (`DELETE FROM auth.users WHERE email='test-ralph@rami.local'` → CASCADE).
5. Si OK → US-011 `passes=true` + commit `[US-011] ... browser-verified`.

Ce compte test servira aussi pour US-012 (dashboard), US-031, US-032 (toutes UI, browser-verify requis).

## brand_dna : shape RÉELLE (plate) — IMPORTANT
Le brand_dna sauvegardé par l'onboarding est **PLAT**, PAS nesté. Clés réelles : `sector`, `primaryCulture`, `objectifCognitif`, `objectifsCognitifs`, `colorPrimary`/`colorSecondary`/`colorAccent`, `brandName`, `tagline`, `positioning`, `voiceTone`, `typography`, `audienceAge`/`audienceLocation`/`audienceDescription`/`audiencePainPoints`, `logoDataUrl`, `sectorCustom`, `perplexity_benchmark`. ⚠️ L'interface `BrandDNA` du prompt-compiler suppose une shape NESTÉE (`identity.sector`, `color_palette[]`, `culture_markets.primary_culture`) → **divergence** : le compiler lit probablement mal les couleurs en prod (dette pré-existante, hors-scope US-009, à traiter dans une story dédiée). Pour US-010 lire le secteur via `brand_dna->>'sector'` et la culture via `brand_dna->>'primaryCulture'`.
- Helper SQL créé (US-009) : `get_current_tenant_sector()` = `brand_dna->>'sector'`.

## Méthode migration db-rami (validée US-007/008/009)
Appliquer une migration SQL sur l'instance dédiée db-rami **sans casser le copier-coller** :
```powershell
. "C:\Users\amans\.claude\secrets\load-secrets.ps1" | Out-Null
$key=$env:SERVER_SSH_KEY;$h=$env:SERVER_HOST;$u=$env:SERVER_SSH_USER
$db="supabase-db-szn6rjsrqig7n4oerw27egwr"
$sql=[IO.File]::ReadAllText("<chemin migration .sql>")
$blob=[Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes($sql))
ssh -i $key -o BatchMode=yes "$u@$h" "echo '$blob' | base64 -d | docker exec -i $db psql -U postgres -d postgres -v ON_ERROR_STOP=1"
```
- Test RLS cross-tenant : `BEGIN; INSERT tenant B temp + lignes A/B; SELECT set_config('request.jwt.claims','{""sub"":""<userA>""}',true); SET LOCAL ROLE authenticated; SELECT ... (doit voir A seul); ROLLBACK;`. `get_current_tenant_id()` = `SELECT tenant_id FROM users WHERE id=auth.uid()`. Tenant réel unique : `ai-mpower` `0f38ef43-7467-494b-a2a4-86a21d3293bd`, user super-admin `6d7bda98-44b0-4735-81f4-e9f7ff8274aa`.
- ⚠️ Dollar-quoting (`$$ ... $$`) cassé par l'échappement PowerShell→ssh : éviter les blocs `DO $$`, préférer des INSERT directs pour tester WITH CHECK.
- `service_role`/Supabase : les grants anon/authenticated/service_role sont **hérités automatiquement** sur les nouvelles tables (ALTER DEFAULT PRIVILEGES Supabase) → pas de GRANT à ajouter.

## Log itérations
<!-- (date | story | fichiers | learnings) -->
- **2026-05-31 | US-001 | Schéma post_metrics + RLS** — `src/lib/db/schema.ts` (table `postMetrics` + types `PostMetric`/`NewPostMetric`, import `real`), `supabase/migrations/20260315000001_post_metrics.sql` (table + 4 policies RLS `tenant_id = get_current_tenant_id()` + 2 index). Appliqué sur db-rami (RLS=t, 4 policies). Gates: TS0/lint0/build OK. *Learning* : schéma réel = `src/lib/db/schema.ts` (pas packages/db) ; migrations dans `supabase/migrations/` ; enum `platform` réutilisé.
- **2026-05-31 | US-002 | MetricsProvider + collecte X (Twitter)** — `src/lib/services/metrics/types.ts` (`NormalizedMetrics`, `MetricsFetchInput`, `MetricsResult` union avec `unavailable`, interface `MetricsProvider`), `engagement.ts` (`computeEngagementRate` borné [0,1]), `twitter.ts` (`fetchTwitterMetrics` → API v2 `GET /2/tweets/:id?tweet.fields=public_metrics,non_public_metrics,organic_metrics`, 401/403/404 → `{unavailable:true}` sans crash), `index.ts` (router `collectMetricsFromPlatform` calqué sur `publishing/index.ts`). Gates: TS0/lint0/build OK. *Learnings* : table OAuth réelle = `oauth_connections` (PAS `social_accounts` du schéma Drizzle, vestige) ; `posts.platform_results` JSONB = `{ twitter: { postId, postUrl, status }, ... }` → source du `platformPostId` pour la collecte ; mapping Twitter→post_metrics : shares = retweet+quote, saves = bookmark_count, clicks = non_public.url_link_clicks. `AbortSignal.timeout(15000)` pour le réseau.
- **2026-05-31 | US-003 | Collecte metrics LinkedIn** — `src/lib/services/metrics/linkedin.ts` (`fetchLinkedInMetrics`) + branché dans le router. Deux chemins selon l'author URN (`accountId`) : organisation (`urn:li:organization:`) → `organizationalEntityShareStatistics` (impressions/likes/comments/shares/clicks réels) ; membre (`urn:li:person:`) → `socialActions/{urn}` (likes+comments uniquement). 401/403 → `{unavailable:true}` (scope absent géré gracieusement). Header `X-Restli-Protocol-Version: 2.0.0`. Gates: TS0/lint0/build OK. *Learning* : impressions d'un post de MEMBRE non exposées par l'API LinkedIn → laissées à 0 (donnée honnête, jamais estimée — DÉFCON 1) ; `platformPostId` LinkedIn = le share/ugcPost URN ; `accountId` = author URN.
- **2026-05-31 | US-004 | Collecte metrics Meta (FB+IG)** — `src/lib/services/metrics/meta.ts` : `fetchFacebookMetrics` (Graph `?fields=likes.summary(true),comments.summary(true),shares,insights.metric(post_impressions,post_impressions_unique,post_clicks)`) + `fetchInstagramMetrics` (2 appels : `?fields=like_count,comments_count` puis `/insights?metric=reach,saved,shares,impressions`). Deux fonctions branchées séparément (`facebook`/`instagram`) dans le router. Token Meta en query param ; 401/403/code 190 → `{unavailable:true}` ; insights IG en échec → dégradation propre (garde likes/comments). Gates: TS0/lint0/build OK. *Learnings* : FB `platformPostId` = `{page}_{post}`, IG = media id ; FB pas de `save` (saves=0), IG pas de `clicks` (clicks=0) ; reach stocké dans `raw` (pas de colonne post_metrics) ; impressions IG fallback = reach si `impressions` déprécié.
- **2026-05-31 | US-005 | Collecte metrics Pinterest** — `src/lib/services/metrics/pinterest.ts` (`fetchPinterestMetrics`) + branché. `GET /v5/pins/{id}/analytics?start_date&end_date&metric_types=IMPRESSION,SAVE,PIN_CLICK,OUTBOUND_CLICK` (fenêtre 30 j obligatoire, max 90). Parsing défensif `summary_metrics` (racine ou `all.summary_metrics`). clicks = PIN_CLICK+OUTBOUND_CLICK ; likes/comments/shares = 0 (Pinterest n'a pas ces metrics). 401/403/404 → `{unavailable:true}`. **Router metrics complet (5 plateformes Phase 1)** : twitter/linkedin/facebook/instagram/pinterest. Gates: TS0/lint0/build OK. *Learning* : Pinterest analytics EXIGE start_date/end_date ; `new Date()` OK en code runtime (interdiction Date.now() concerne uniquement les scripts Workflow).
- **2026-05-31 | US-006 | Job pg-boss collecte planifiée** — `src/lib/queue/token-refresh.ts` (extraction DRY de `getValidAccessToken` depuis publish-worker, partagé par les 2 workers), `src/lib/queue/jobs/collect-metrics.ts` (`startCollectMetricsWorker` + `processCollectMetricsJob` : lit `posts.platform_results` → pour chaque plateforme publiée, conn OAuth active → token → `collectMetricsFromPlatform` → INSERT snapshot `post_metrics`), `pgboss.ts` (job `COLLECT_METRICS` + `CollectMetricsPayload{postId,tenantId,window}` + `scheduleMetricsCollection` T+1h/24h/7j, retry x3 backoff, singletonKey `metrics:{post}:{window}`), `instrumentation.ts` (démarre le worker), publish-worker (étape 8 : `scheduleMetricsCollection` best-effort après `published`). Gates: TS0/lint0/build OK. *Learnings* : `platformPostId` source = `posts.platform_results[platform].postId` (PublishResult) ; collecte = INSERT snapshot horodaté (série temporelle, pas écrasement) ; `unavailable` (token révoqué) → skip sans retry, seules les erreurs DB transitoires throw pour déclencher le retry pg-boss ; `boss.send(startAfter: seconds)` accepte un nombre de secondes.
- **2026-05-31 | US-007 | Attribution feature→performance** — migration `supabase/migrations/20260315000002_attribution.sql` (**appliquée sur db-rami**, RLS testée cross-tenant ✅ : SELECT isolé + INSERT WITH CHECK bloqué) : VIEW `attribution_facts` `security_invoker=true` (dernier snapshot par post×plateforme + dimensions `ai_metadata`) + table `attribution_rankings` (4 policies RLS). `src/lib/db/schema.ts` (+`attributionRankings`, import `bigint`). `src/lib/services/metrics/attribution.ts` (`aggregateAttribution` pur + `topFeatures(tenantId,sector,dimension)` live, `MIN_SAMPLE_SIZE=3`). `src/lib/queue/jobs/attribution-refresh.ts` (cron 6h incrémental → recalcule cache via service client, purge+insert par tenant). pgboss `ATTRIBUTION_REFRESH` + instrumentation. Gates: TS0/lint0/build OK + RLS db. *Learnings* : `generated_assets` n'existe pas → dimensions dans `posts.ai_metadata` (NULL honnête si absent) ; vue `security_invoker` hérite RLS sans policy ; `NewAttributionRanking.computed_at` = Date (mode Drizzle) → laisser défaut DB `now()` ; lien post→`visual_session_images` flou (URL, pas FK) → non utilisé MVP.
- **2026-05-31 | US-008 | Performance prior dans Prompt Compiler** — `prompt-compiler.ts` (type `PerformancePrior` + 5e param `performancePrior`, couleurs gagnantes en tête de `effectiveColors` + `performanceKW` style gagnant injecté au prompt positif). `attribution.ts` (`buildPerformancePrior(tenantId,sector)` via `topFeatures` couleurs+styles, gaté `MIN_PRIOR_SAMPLE=10` → `null` sinon = Causse pur, FR-2). `visual.actions.ts` (construit le prior, le passe au compiler + à `persistVisualSession`). Migration `20260315000003_performance_prior.sql` (colonne `visual_session_images.performance_prior` jsonb, **appliquée db-rami**). Gates: TS0/lint0/build OK. *Learnings* : `PerformancePrior` défini dans le compiler (pur) pour éviter un cycle (attribution importe le TYPE) ; ajout de colonne sur table RLS existante → hérite la RLS, pas de test cross-tenant séparé.
- **2026-05-31 | US-009 | Schéma collective_benchmarks + anonymisation** — migration `20260315000004_collective_benchmarks.sql` (**appliquée db-rami**, **3 tests sécurité passés** : isolation secteur ✅, insert authenticated bloqué ✅, k-anonymity CHECK ✅). Helper `get_current_tenant_sector()` (SECURITY DEFINER, `brand_dna->>'sector'`). Table `collective_benchmarks(sector,culture,platform,metric,value,sample_size,updated_at)` SANS tenant_id (cross-tenant) + `CONSTRAINT CHECK(sample_size>=5)` + unique `(sector,culture,platform,metric)`. RLS : 1 seule policy SELECT `sector=get_current_tenant_sector()` ; aucune policy write → authenticated bloqué, service_role bypass. `src/lib/db/schema.ts` (+`collectiveBenchmarks`). Gates: TS0/lint0/build OK. *Learnings* : secteur réel = `brand_dna->>'sector'` (PLAT) ; service_role bypass RLS confirmé (write ok sans policy) ; CHECK k-anonymity = garde-fou structurel en plus du filtre job.
